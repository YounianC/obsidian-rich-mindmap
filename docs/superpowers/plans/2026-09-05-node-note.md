# 节点备注（引用块存储，悬浮展示）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给思维导图节点加多行备注：存成节点行下方的 Markdown 引用块，导图上以角标提示、悬浮角标弹气泡展示，选中节点后从工具栏打开多行输入框编辑。

**Architecture:** 备注是紧跟节点行的连续引用行（`> …`），解析期由 `splitNote` 从 `continuation` 里摘进 `MindNode.note`，序列化期原样重放（`Note.raw`）或按当前缩进重新生成（`raw === null`）。视图层新增一个角标、一个悬浮气泡模块、一个 `<textarea>` 浮层模块，全部走既有的 `placeNear` / `mm-no-pan` / `t()` 约定。

**Tech Stack:** TypeScript（`strict: true`，禁用 `any`）、vitest + fast-check、esbuild、Obsidian API。

设计文档：[docs/superpowers/specs/2026-09-05-node-note-design.md](../specs/2026-09-05-node-note-design.md)

## Global Constraints

这些约束来自 [AGENTS.md](../../../AGENTS.md)，每一条都对应过一个真实发生并被修掉的缺陷。**每个任务的要求都隐含包含本节全部内容。**

- **纯函数层边界**：`src/model/**` 与 `src/view/layout.ts`、`src/view/camera.ts` 不得 `import obsidian`，不得出现 `document` / `window` / `HTMLElement`。`npm run check:purity` 强制校验。本计划新增的 `src/model/note.ts` 落在这条边界内。
- **只允许五条写回归一化**：补尾换行、松散列表压缩、CRLF→LF、缩进单位归纳、行内标记排序。**其余任何字节差异都是缺陷。** 判据是 `tests/roundtrip.test.ts`，其中每条语料断言逐字节相等。**不要为了让测试过而放宽生成器或语料。**
- **面向用户的字符串一律走 `t()`**（`src/i18n.ts`）。中文表是 key 的唯一来源，英文表声明为 `Record<MessageKey, string>`，漏译是编译错误。`npm run check:i18n` 扫 `src/main.ts` / `src/settings.ts` / `src/view.ts` / `src/view/**` 的字符串字面量，发现中文即失败。**注释不受约束，只有字面量受约束。**
- **TS 里不写颜色字面量**；CSS 颜色取自 Obsidian 变量或本插件的 `--mm-*` 调色板。阴影用 `var(--shadow-s)`。
- **`closest()` 之前的类型守卫必须是 `instanceof Element`，不是 `instanceof HTMLElement`。** 本项目自己画的角标是内联 SVG，`event.target` 会是 `SVGElement`。
- **任何浮在画布之上的元素都必须带 `mm-no-pan` 类**，否则在它上面按下拖拽会连带平移底下的画布。
- **`this.root` 上的监听器注册只能放在唯一的 `if (!this.eventsAttached)` 块里**，否则会堆叠监听器。
- **事件传播**：`stopPropagation()` 之前必须放行带修饰键的按键（`metaKey || ctrlKey || altKey`），否则会挡掉 Obsidian 全局热键。
- **视图层不重新推导「哪些操作可用」**：能力判断一律从 `tree-ops` 的查询函数来。
- **不用 `innerHTML`**：用户文字一律 `createElement` + `textContent` 拼装。
- 每个任务结束前跑门禁：`npm run typecheck && npm test && npm run build && npm run check:purity && npm run check:i18n`。

## 文件结构

| 文件 | 动作 | 职责 |
| --- | --- | --- |
| `src/model/types.ts` | 修改 | 新增 `Note` 接口与 `MindNode.note` 可选字段 |
| `src/model/note.ts` | 新建 | 纯函数：`splitNote`（续行 → 备注 + 剩余）、`renderNote`（正文 → 待写出的行） |
| `src/model/parser.ts` | 修改 | 构造节点时调 `splitNote`；根节点不调 |
| `src/model/serializer.ts` | 修改 | 节点行之后写出备注行 |
| `src/model/tree-ops.ts` | 修改 | `setNote` / `canNote`；`moveNode` 清 `note.raw` |
| `src/i18n.ts` | 修改 | 5 个新 key，中英各一份 |
| `src/view/node-el.ts` | 修改 | `buildNoteBadge`；`buildMarks` 改接 `MindNode`；导出 `renderInline` |
| `src/view/note-tip.ts` | 新建 | 悬浮气泡：委托监听 + 延迟开关 + 行内渲染 |
| `src/view/note-popover.ts` | 新建 | `<textarea>` 编辑浮层 |
| `src/view/toolbar.ts` | 修改 | 「备注」按钮与 `canNote` 能力位 |
| `src/view.ts` | 修改 | 接线：能力位、`openNote`、气泡图层、生命周期清理 |
| `styles.css` | 修改 | 角标、气泡、编辑浮层的样式 |
| `tests/note.test.ts` | 新建 | `splitNote` / `renderNote` 的正确性与对抗性测试 |
| `tests/roundtrip.test.ts` | 修改 | 带引用块的语料；`stripIds` 纳入 note；fuzz 生成备注 |
| `tests/tree-ops.test.ts` | 修改 | `setNote` / `canNote` / `moveNode` 的 raw 失效 |
| `AGENTS.md`、`README.md`、`README.zh-CN.md`、`docs/MANUAL-VERIFICATION.md` | 修改 | 约束、功能说明、人工验证清单 |

---

### Task 1: `Note` 类型与 `src/model/note.ts`

**Files:**
- Modify: `src/model/types.ts`
- Create: `src/model/note.ts`
- Test: `tests/note.test.ts`

**Interfaces:**
- Consumes: 无（本任务是最底层）
- Produces:
  - `interface Note { text: string; raw: string[] | null }`（`src/model/types.ts`）
  - `MindNode.note?: Note`
  - `splitNote(continuation: readonly string[]): { note: Note | null; rest: string[] }`
  - `renderNote(text: string, indent: string): string[]`

- [ ] **Step 1: 在 `src/model/types.ts` 里加 `Note` 接口**

加在 `MindNode` 接口**之前**（`Bullet` / `HeadingForm` 之后）：

```ts
/**
 * 节点备注：紧跟节点行的连续引用行（`> …`），解析期由 note.ts 的 splitNote
 * 从 `continuation` 里摘出来。
 *
 * `raw` 是这个设计的承重墙。用户写的可能是 `>备注`（无空格）、`>  备注`
 * （两个空格）、tab 缩进。只存解码后的 `text`、写回时统一渲染成 `> `，就等于
 * 给项目加了第六条写回归一化——违反 AGENTS.md 第 2 条。所以解析期把原始行
 * 整条留住，**只有用户真正改了备注才置 `raw = null`**，那时才由 serialize
 * 按节点当前的缩进重新生成。
 */
export interface Note {
  /** 去掉缩进与 `>` 前缀后的备注正文，行间以 \n 连接 */
  text: string;
  /** 来自文件的原始行（含缩进与前缀），逐字重放；用户编辑后为 null */
  raw: string[] | null;
}
```

- [ ] **Step 2: 在 `MindNode` 接口里加 `note` 字段**

加在 `heading?: HeadingForm;` **之前**：

```ts
  /** 存在即为「这个节点带备注」。根节点恒无此字段：parser 不对根做 splitNote，
   *  tree-ops.setNote 对根 id 是 no-op（理由与 setMarks 相同）。 */
  note?: Note;
```

- [ ] **Step 3: 写失败的测试 `tests/note.test.ts`**

```ts
import { describe, expect, it } from "vitest";
import fc from "fast-check";
import { renderNote, splitNote } from "../src/model/note";

describe("splitNote", () => {
  it("收编开头连续的引用行，遇到非引用行截断", () => {
    const result = splitNote(["  > 第一行", "  > 第二行", "  普通续行"]);
    expect(result.note).toEqual({
      text: "第一行\n第二行",
      raw: ["  > 第一行", "  > 第二行"],
    });
    expect(result.rest).toEqual(["  普通续行"]);
  });

  it("开头不是引用行时不收编，续行原样返回", () => {
    const result = splitNote(["  普通续行", "  > 不是备注"]);
    expect(result.note).toBeNull();
    expect(result.rest).toEqual(["  普通续行", "  > 不是备注"]);
  });

  it("空续行返回空备注", () => {
    expect(splitNote([])).toEqual({ note: null, rest: [] });
  });

  it("`>` 后没有空格也认，且 raw 逐字保留", () => {
    const result = splitNote(["  >备注"]);
    expect(result.note).toEqual({ text: "备注", raw: ["  >备注"] });
  });

  it("`>` 后有两个空格时只吃掉一个，第二个属于正文", () => {
    expect(splitNote(["  >  备注"]).note?.text).toBe(" 备注");
  });

  it("tab 缩进同样认", () => {
    expect(splitNote(["\t> 备注"]).note?.text).toBe("备注");
  });

  it("`>` 单独成行解码成空行", () => {
    expect(splitNote(["  > 上", "  >", "  > 下"]).note?.text).toBe("上\n\n下");
  });

  it("callout 一律按引用行收编，不做特判", () => {
    const result = splitNote(["  > [!note] 提示", "  > 内容"]);
    expect(result.note?.text).toBe("[!note] 提示\n内容");
  });
});

describe("renderNote", () => {
  it("每行加缩进与 `> ` 前缀", () => {
    expect(renderNote("第一行\n第二行", "  ")).toEqual(["  > 第一行", "  > 第二行"]);
  });

  it("空行写成 `indent + \">\"` 而不是真空行", () => {
    // 真空行会把一个引用块劈成两个，重新解析时 splitNote 只收到前半段，
    // 后半段掉进 continuation 变成不可见正文——一次编辑静默吃掉半条备注。
    expect(renderNote("上\n\n下", "\t")).toEqual(["\t> 上", "\t>", "\t> 下"]);
  });
});

describe("renderNote → splitNote 往返", () => {
  it("对任意正文成立", () => {
    const bodyArb = fc
      .array(
        fc.constantFrom("普通文字", "", " 前导空格", "> 引用", "- 列表", "# 标题", "```"),
        { minLength: 1, maxLength: 6 },
      )
      .map((lines) => lines.join("\n"));

    fc.assert(
      fc.property(bodyArb, fc.constantFrom("", "  ", "    ", "\t"), (body, indent) => {
        const back = splitNote(renderNote(body, indent));
        expect(back.note?.text).toBe(body);
        expect(back.rest).toEqual([]);
      }),
      { numRuns: 300 },
    );
  });
});
```

- [ ] **Step 4: 运行测试确认失败**

Run: `npx vitest run tests/note.test.ts`
Expected: FAIL，报 `Failed to resolve import "../src/model/note"`。

- [ ] **Step 5: 写 `src/model/note.ts`**

```ts
import type { Note } from "./types";

/**
 * 引用行：可有缩进，`>` 之后最多吃掉一个空白字符。
 *
 * 只吃一个是刻意的：`>  备注` 的第二个空格属于正文，吃掉它会让「写回时补回
 * 一个空格」与原文对不上，进而在 renderNote 重新生成时产生字节差异。
 */
const QUOTE_RE = /^[ \t]*>[ \t]?(.*)$/;

/**
 * 从续行**开头**切出备注。不是开头的引用块不收——备注在语义上必须紧贴节点行，
 * 中间隔了散文之后的引用块是普通正文，收编它会让「哪块是备注」变得不可预测。
 *
 * `note.raw` 是切下来的原始行，逐字保留（见 types.ts 的 Note 说明）。
 */
export function splitNote(continuation: readonly string[]): {
  note: Note | null;
  rest: string[];
} {
  const decoded: string[] = [];
  let end = 0;
  while (end < continuation.length) {
    const match = QUOTE_RE.exec(continuation[end]);
    if (match === null) break;
    decoded.push(match[1]);
    end++;
  }
  if (end === 0) return { note: null, rest: [...continuation] };
  return {
    note: { text: decoded.join("\n"), raw: continuation.slice(0, end) },
    rest: continuation.slice(end),
  };
}

/**
 * 备注正文 → 待写出的行。
 *
 * 空行写成 `indent + ">"` 而不是真空行：真空行会把一个引用块劈成两个，重新
 * 解析时 splitNote 只会收到前半段，后半段掉进 continuation 变成图上不可见的
 * 正文——一次编辑静默吃掉半条备注。
 */
export function renderNote(text: string, indent: string): string[] {
  return text
    .split("\n")
    .map((line) => (line === "" ? `${indent}>` : `${indent}> ${line}`));
}
```

- [ ] **Step 6: 运行测试确认通过**

Run: `npx vitest run tests/note.test.ts`
Expected: PASS，10 个用例全绿。

- [ ] **Step 7: 跑门禁**

Run: `npm run typecheck && npm test && npm run check:purity`
Expected: 全绿。`check:purity` 必须把新的 `src/model/note.ts` 纳入扫描且通过（它只 import 类型，不碰 DOM）。

- [ ] **Step 8: 提交**

```bash
git add src/model/types.ts src/model/note.ts tests/note.test.ts
git commit -m "feat: 备注的解析与渲染纯函数（splitNote / renderNote）"
```

---

### Task 2: parser 收编备注、serializer 写出备注

**Files:**
- Modify: `src/model/parser.ts`（`buildTree` 里两处节点构造）
- Modify: `src/model/serializer.ts`（`serializeNodes` 两个分支）
- Test: `tests/roundtrip.test.ts`（新增语料、`stripIds` 纳入 note）

**Interfaces:**
- Consumes: `splitNote` / `renderNote`（Task 1）、`Note`、`MindNode.note`
- Produces: `parse()` 产出的节点带 `note` 字段；`serialize()` 把 `note` 写回节点行下方。后续任务依赖「`parse → serialize` 逐字节相等」这个性质。

- [ ] **Step 1: 先加失败的往返语料**

在 `tests/roundtrip.test.ts` 的 `CORPUS` 数组末尾（最后一条 `"# t\n\n- a\n## B\n\n- b\n\t- c\n"` 之后）加入：

```ts
  // 备注：节点行下方的连续引用行。前几条覆盖「原样重放」的各种书写形态，
  // 后几条覆盖收编边界（非开头的引用块不收、根节点不收）。
  "# t\n\n- a\n  > 备注\n",
  "# t\n\n- a\n  > 第一行\n  > 第二行\n- b\n",
  "# t\n\n- a\n  >备注\n",
  "# t\n\n- a\n  >  备注\n",
  "# t\n\n- a\n  > 上\n  >\n  > 下\n",
  "# t\n\n- a\n\t> 备注\n\t- b\n",
  "# t\n\n- a\n  > [!note] 提示\n  > 内容\n",
  "# t\n\n- a\n  > 备注\n  普通续行\n- b\n",
  "# t\n\n- a\n  普通续行\n  > 不是备注\n- b\n",
  "# t\n\n## A\n> 标题的备注\n\n- a\n",
  "# t\n\n> 根下面的引用块\n\n- a\n",
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run tests/roundtrip.test.ts`
Expected: 全部 11 条新语料 FAIL 吗？——**不会**。这一步的预期是 **PASS**：备注行此刻还留在 `continuation` 里被原样重放，字节本来就相等。这条语料的价值是**回归护栏**：它保证接下来改 parser/serializer 时字节不变。请确认它们确实是 PASS，若有 FAIL 说明语料本身不是规范形态，先修语料。

- [ ] **Step 3: 提交语料**

```bash
git add tests/roundtrip.test.ts
git commit -m "test: 备注引用块的往返字节语料（改动前的回归护栏）"
```

- [ ] **Step 4: 加结构断言，确认它此刻失败**

在 `tests/roundtrip.test.ts` 的 `describe("serialize(parse(md)) === md", …)` 之后新增：

```ts
describe("备注从续行里被摘进 node.note", () => {
  it("列表项的备注", () => {
    const doc = parse("# t\n\n- a\n  > 第一行\n  > 第二行\n  普通续行\n", "x.md");
    const a = doc.root.children[0];
    expect(a.note).toEqual({
      text: "第一行\n第二行",
      raw: ["  > 第一行", "  > 第二行"],
    });
    expect(a.continuation).toEqual(["  普通续行"]);
  });

  it("标题节点的备注", () => {
    const doc = parse("# t\n\n## A\n> 标题的备注\n\n- a\n", "x.md");
    const heading = doc.root.children[0];
    expect(heading.note?.text).toBe("标题的备注");
    expect(heading.continuation).toEqual([""]);
  });

  it("根节点不收编：引用块留在 continuation 里", () => {
    const doc = parse("# t\n\n> 根下面的引用块\n\n- a\n", "x.md");
    expect(doc.root.note).toBeUndefined();
    expect(doc.root.continuation).toEqual(["", "> 根下面的引用块", ""]);
  });

  it("非开头的引用块不收编", () => {
    const doc = parse("# t\n\n- a\n  普通续行\n  > 不是备注\n", "x.md");
    expect(doc.root.children[0].note).toBeUndefined();
  });
});
```

Run: `npx vitest run tests/roundtrip.test.ts`
Expected: FAIL —— 前两个用例报 `expected undefined to deeply equal …`（parser 还没调 `splitNote`）；后两个 PASS。

- [ ] **Step 5: 改 `src/model/parser.ts`：标题分支**

在文件顶部的 import 里加：

```ts
import { splitNote } from "./note";
```

在 `buildTree` 的标题分支里，把节点构造改成先切备注、再构造、最后按需挂上：

```ts
      const headingMarks = parseMarks(entry.text);
      const headingNote = splitNote(entry.continuation);
      const node: MindNode = {
        id: `n${nextId++}`,
        text: headingMarks.rest,
        marks: headingMarks.marks,
        children: [],
        collapsed: false,
        continuation: headingNote.rest,
        bullet: "-",
        heading: {
          level: entry.level,
          prefix: entry.prefix,
          suffix: entry.suffix,
          indentUnit: null,
        },
      };
      // 只在有备注时挂字段：`note: null` 与「没有备注」是两种状态，让不带备注的
      // 节点根本不出现这个键，结构比较与 JSON 快照都更干净。
      if (headingNote.note !== null) node.note = headingNote.note;
```

- [ ] **Step 6: 改 `src/model/parser.ts`：列表项分支**

把列表项分支的节点构造改成：

```ts
    const { marks, rest } = parseMarks(entry.text);
    const itemNote = splitNote(entry.continuation);

    const node: MindNode = {
      id: `n${nextId++}`,
      text: rest,
      marks,
      children: [],
      collapsed: false,
      continuation: itemNote.rest,
      bullet: entry.bullet,
    };
    if (itemNote.note !== null) node.note = itemNote.note;
```

- [ ] **Step 7: 确认根节点分支保持不变**

`parse()` 里构造 `root` 的那段（`continuation: hasHeading ? rootEntry.continuation : []`）**不要动**，并在它上方补一句注释：

```ts
    // 根节点不做 splitNote：根不支持备注（tree-ops.setNote 对根 id 是 no-op，
    // 理由与 setMarks 相同），若解析期仍把根下面的引用块收成备注，导图上会出现
    // 一个点不动的角标。与「parse 不对根的 H1 调 parseMarks」逐字对称。
```

- [ ] **Step 8: 运行测试，确认结构断言过、往返语料挂**

Run: `npx vitest run tests/roundtrip.test.ts`
Expected: 「备注从续行里被摘进 node.note」4 个用例 PASS；`CORPUS` 里带备注的语料 FAIL（备注行从 `continuation` 里消失了，serializer 还没写它们）。

- [ ] **Step 9: 改 `src/model/serializer.ts`**

在 import 里加：

```ts
import { renderNote } from "./note";
```

在 `composeLine` 之后加一个辅助函数：

```ts
/**
 * 写出节点的备注行。`raw` 非 null 时逐字重放（保证往返字节相等）；用户编辑过
 * 备注（`raw === null`）时才按 `indent` 重新生成——缩进只有序列化期知道，
 * 这里是唯一该做这件事的地方。
 */
function serializeNote(node: MindNode, indent: string): string {
  if (node.note === undefined) return "";
  const lines = node.note.raw ?? renderNote(node.note.text, indent);
  let out = "";
  for (const line of lines) out += `${line}\n`;
  return out;
}
```

在 `serializeNodes` 的标题分支里，把

```ts
      out += `${node.heading.prefix}${composeLine(node)}${node.heading.suffix}\n`;
      for (const line of node.continuation) out += `${line}\n`;
```

改成

```ts
      out += `${node.heading.prefix}${composeLine(node)}${node.heading.suffix}\n`;
      // 标题行顶格，它的备注也顶格。
      out += serializeNote(node, "");
      for (const line of node.continuation) out += `${line}\n`;
```

在列表项分支里，把

```ts
    out += `${indentUnit.repeat(listDepth)}${node.bullet} ${composeLine(node)}\n`;
    for (const line of node.continuation) out += `${line}\n`;
```

改成

```ts
    out += `${indentUnit.repeat(listDepth)}${node.bullet} ${composeLine(node)}\n`;
    // 比节点自己深一层，备注才落在这个列表项内部而不是掉出列表。
    out += serializeNote(node, indentUnit.repeat(listDepth + 1));
    for (const line of node.continuation) out += `${line}\n`;
```

在 `serialize()` 里根节点标题行那段的注释后面补一句：

```ts
    // 根节点没有备注：parse 不对根做 splitNote，setNote 对根 id 是 no-op，
    // 所以这里不需要 serializeNote。
```

- [ ] **Step 10: 运行测试确认往返恢复**

Run: `npx vitest run tests/roundtrip.test.ts`
Expected: PASS，全部语料逐字节相等。

- [ ] **Step 11: 让结构比较覆盖 note**

在 `tests/roundtrip.test.ts` 的 `stripIds` 里加一行（放在 `heading` 之后）：

```ts
    // 只比 text 不比 raw：raw 的字节保真由上面的 CORPUS 逐字节断言覆盖，
    // 而 fuzz 生成的备注 raw 恒为 null、往返回来必然带上真实行，比它会产生假反例。
    note: node.note?.text ?? null,
```

Run: `npx vitest run tests/roundtrip.test.ts`
Expected: PASS。

- [ ] **Step 12: 跑门禁并提交**

Run: `npm run typecheck && npm test && npm run build && npm run check:purity && npm run check:i18n`
Expected: 全绿。

```bash
git add src/model/parser.ts src/model/serializer.ts tests/roundtrip.test.ts
git commit -m "feat: 解析与写回节点备注（引用块 ↔ MindNode.note）"
```

---

### Task 3: 对抗性与属性测试

**Files:**
- Modify: `tests/note.test.ts`（新增「不长出假节点」的对抗性断言）
- Modify: `tests/roundtrip.test.ts`（fuzz 生成器带上备注）

**Interfaces:**
- Consumes: `renderNote`（Task 1）、`parse` / `serialize` 带备注的行为（Task 2）
- Produces: 无新接口。这是把「`>` 前缀即转义」这个论证变成可执行断言。

- [ ] **Step 1: 在 `tests/note.test.ts` 里加对抗性断言**

先在文件顶部补 import：

```ts
import { parse } from "../src/model/parser";
import type { MindNode } from "../src/model/types";
```

在文件末尾追加：

```ts
function countNodes(node: MindNode): number {
  return 1 + node.children.reduce((sum, child) => sum + countNodes(child), 0);
}

/**
 * 对抗性测试：备注正文里写什么都不能改变导图的节点数。
 *
 * 这是「`>` 前缀天然是逃逸字符」这个论证的可执行版本。只有正确性用例不够——
 * 门禁看不见「某个输入让备注长出一个假节点、或打开一个假围栏」这类问题。
 * AGENTS.md：手写 parser 只交正确性测试不够，必须同时带对抗性输入测试。
 */
describe("备注正文不改变导图结构", () => {
  it("对任意正文成立", () => {
    const hostileLine = fc.constantFrom(
      "- 列表项",
      "* 星号列表",
      "+ 加号列表",
      "  - 缩进列表项",
      "# 一级标题",
      "## 二级标题",
      "###### 六级标题",
      "```",
      "```js",
      "~~~",
      "> 引用",
      ">> 双层引用",
      "1. 有序列表",
      "普通文字",
      "",
      "\t- tab 缩进列表项",
      "---",
    );
    const bodyArb = fc
      .array(hostileLine, { minLength: 1, maxLength: 8 })
      .map((lines) => lines.join("\n"));

    const baseline = countNodes(parse("# t\n\n- a\n- b\n", "x.md").root);

    fc.assert(
      fc.property(bodyArb, (body) => {
        const note = renderNote(body, "  ").join("\n");
        const md = `# t\n\n- a\n${note}\n- b\n`;
        const doc = parse(md, "x.md");
        expect(countNodes(doc.root)).toBe(baseline);
        // 备注必须整条落在 a 身上，一个字符都不能漏进别处。
        expect(doc.root.children[0].note?.text).toBe(body);
      }),
      { numRuns: 500 },
    );
  });
});
```

- [ ] **Step 2: 运行确认通过**

Run: `npx vitest run tests/note.test.ts`
Expected: PASS。若 FAIL，**不要放宽 `hostileLine` 的取值** —— 那说明 `renderNote` 的转义有洞，去修 `renderNote`。

- [ ] **Step 3: 让往返 fuzz 生成备注**

在 `tests/roundtrip.test.ts` 里，`continuationArb` 定义之后加：

```ts
/** 备注正文：留空（不带备注），或几种含空行、含标记字符的真实取值。 */
const noteArb = fc.constantFrom(
  undefined,
  { text: "一行备注", raw: null },
  { text: "第一行\n第二行", raw: null },
  { text: "上\n\n下", raw: null },
  { text: "带 **粗体** 与 [[链接]]", raw: null },
);
```

在 `itemNodeArb` 的 `fc.record({…})` 里加一项（放在 `bullet` 之后）：

```ts
    note: noteArb,
```

在 `headingNodeArb` 的 `fc.record({…})` 里同样加（放在 `bullet` 之后、`heading` 之前）：

```ts
      note: noteArb,
```

- [ ] **Step 4: 运行属性测试**

Run: `npx vitest run tests/roundtrip.test.ts`
Expected: PASS。`stripIds` 只比 `note?.text`（Task 2 Step 11 已改），生成的 `raw: null` 往返后变成真实行不构成反例。

- [ ] **Step 5: 跑门禁并提交**

Run: `npm run typecheck && npm test`
Expected: 全绿。

```bash
git add tests/note.test.ts tests/roundtrip.test.ts
git commit -m "test: 备注的对抗性输入与往返属性测试"
```

---

### Task 4: tree-ops 的备注编辑操作

**Files:**
- Modify: `src/model/tree-ops.ts`
- Test: `tests/tree-ops.test.ts`

**Interfaces:**
- Consumes: `MindNode.note`（Task 1）、`parse`（Task 2 的收编行为）
- Produces:
  - `canNote(root: MindNode, id: string): boolean`
  - `setNote(root: MindNode, id: string, text: string): MindNode`
  - `moveNode` 的新行为：移动子树内所有节点的 `note.raw` 置 null

- [ ] **Step 1: 写失败的测试**

在 `tests/tree-ops.test.ts` 末尾追加（若文件顶部尚未 import `parse`，一并补上 `import { parse } from "../src/model/parser";`）：

```ts
describe("setNote / canNote", () => {
  it("给节点写入备注", () => {
    const doc = parse("# t\n\n- a\n", "x.md");
    const a = doc.root.children[0];
    const next = setNote(doc.root, a.id, "第一行\n第二行");
    expect(next.children[0].note).toEqual({ text: "第一行\n第二行", raw: null });
  });

  it("覆盖已有备注时 raw 置 null，写回按当前缩进重新生成", () => {
    const doc = parse("# t\n\n- a\n  >旧备注\n", "x.md");
    const a = doc.root.children[0];
    expect(a.note?.raw).toEqual(["  >旧备注"]);
    const next = setNote(doc.root, a.id, "新备注");
    expect(next.children[0].note).toEqual({ text: "新备注", raw: null });
  });

  it("空白文本即删除备注", () => {
    const doc = parse("# t\n\n- a\n  > 备注\n", "x.md");
    const a = doc.root.children[0];
    const next = setNote(doc.root, a.id, "   \n  ");
    expect(next.children[0].note).toBeUndefined();
  });

  it("对根 id 是 no-op", () => {
    const doc = parse("# t\n\n- a\n", "x.md");
    expect(setNote(doc.root, doc.root.id, "备注")).toBe(doc.root);
  });

  it("canNote：根不可，其余可", () => {
    const doc = parse("# t\n\n- a\n", "x.md");
    expect(canNote(doc.root, doc.root.id)).toBe(false);
    expect(canNote(doc.root, doc.root.children[0].id)).toBe(true);
    expect(canNote(doc.root, "不存在的 id")).toBe(false);
  });
});

describe("备注不让节点变成不可删/不可拖", () => {
  it("只带备注的节点 hasHiddenContent 为 false", () => {
    const doc = parse("# t\n\n- a\n  > 备注\n", "x.md");
    const a = doc.root.children[0];
    expect(hasHiddenContent(a)).toBe(false);
    expect(canRemove(doc.root, a.id)).toBe(true);
  });
});

describe("moveNode 使备注的 raw 失效", () => {
  it("移动子树内所有节点的 raw 都置 null", () => {
    const doc = parse("# t\n\n- a\n  > A 的备注\n  - b\n    > B 的备注\n- c\n", "x.md");
    const a = doc.root.children[0];
    const b = a.children[0];
    const c = doc.root.children[1];

    const next = moveNode(doc.root, a.id, c.id, 0);
    const movedA = next.children[0].children[0];
    expect(movedA.id).toBe(a.id);
    expect(movedA.note).toEqual({ text: "A 的备注", raw: null });
    expect(movedA.children[0].note).toEqual({ text: "B 的备注", raw: null });
  });

  it("没被移动的节点保留原始 raw", () => {
    const doc = parse("# t\n\n- a\n  > A 的备注\n- c\n  > C 的备注\n", "x.md");
    const a = doc.root.children[0];
    const c = doc.root.children[1];
    const next = moveNode(doc.root, a.id, c.id, 0);
    expect(next.children[0].note?.raw).toEqual(["  > C 的备注"]);
  });
});
```

补齐 import：`setNote`、`canNote`、`hasHiddenContent`、`canRemove`、`moveNode` 都要在文件顶部的 `from "../src/model/tree-ops"` 里。

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run tests/tree-ops.test.ts`
Expected: FAIL，报 `setNote is not exported` / `canNote is not exported`。

- [ ] **Step 3: 在 `src/model/tree-ops.ts` 里加 `canNote`**

放在 `canMark` 之后：

```ts
/** 能否加备注：根节点不能，见 setNote 的说明。 */
export function canNote(root: MindNode, id: string): boolean {
  return id !== root.id && findNode(root, id) !== null;
}
```

- [ ] **Step 4: 加 `setNote`**

放在 `toggleMark` 之后：

```ts
/**
 * 写入或清除节点备注。文本去空白后为空即删除备注。
 *
 * 根节点不带备注，理由与 setMarks 相同：文件没有 H1 行时（`MindDoc.hasHeading`
 * 为假）根本没有可写的位置，`serialize` 会跳过整行，备注静默丢失。所以在变更
 * 源头拒绝，`parse` 也对称地不对根做 splitNote。
 *
 * `raw` 恒为 null：用户改过的备注不能再重放旧字节，交给 serialize 按节点当前的
 * 缩进重新生成（见 serializer 的 serializeNote）。
 */
export function setNote(root: MindNode, id: string, text: string): MindNode {
  if (id === root.id) return root;
  return mapTree(root, (node) => {
    if (node.id !== id) return null;
    if (text.trim() === "") {
      const next = { ...node };
      delete next.note;
      return next;
    }
    return { ...node, note: { text, raw: null } };
  });
}
```

- [ ] **Step 5: 加 `clearNoteRaw` 并接进 `moveNode`**

在 `moveNode` **之前**加：

```ts
/**
 * 深拷贝一棵子树并让其中所有备注的 `raw` 失效。
 *
 * 移动之后节点深度变了，旧缩进不再对；不置 null 的话备注行会留在原来的缩进
 * 列上——仍然解析回同一个节点（续行 sink 收一切非节点行），但在 Obsidian 自己的
 * 阅读视图里会掉出列表外。这是显式编辑触发的字节变化，不受「只允许五条写回
 * 归一化」约束（那五条约束的是「打开再切走」的空操作）。
 */
function clearNoteRaw(node: MindNode): MindNode {
  const next: MindNode = { ...node, children: node.children.map(clearNoteRaw) };
  if (next.note !== undefined) next.note = { ...next.note, raw: null };
  return next;
}
```

在 `moveNode` 最后的 `children.splice(...)` 一行里，把 `moving` 换成 `clearNoteRaw(moving)`：

```ts
    children.splice(Math.max(0, Math.min(index, limit)), 0, clearNoteRaw(moving));
```

- [ ] **Step 6: 运行测试确认通过**

Run: `npx vitest run tests/tree-ops.test.ts`
Expected: PASS。

- [ ] **Step 7: 跑门禁并提交**

Run: `npm run typecheck && npm test && npm run check:purity`
Expected: 全绿。

```bash
git add src/model/tree-ops.ts tests/tree-ops.test.ts
git commit -m "feat: tree-ops 的 setNote / canNote，moveNode 使备注 raw 失效"
```

---

### Task 5: i18n 文案与节点上的备注角标

**Files:**
- Modify: `src/i18n.ts`
- Modify: `src/view/node-el.ts`
- Modify: `styles.css`

**Interfaces:**
- Consumes: `MindNode.note`（Task 1）
- Produces:
  - i18n key：`badge.note`、`toolbar.note`、`toolbar.note.blockedRoot`、`note.placeholder`、`note.hint`
  - `buildNoteBadge(parent: HTMLElement): HTMLElement`（`src/view/node-el.ts` 导出）
  - `renderInline(tokens: InlineToken[], parent: HTMLElement, onOpenLink?: OnOpenLink): void`（改为**导出**，供 Task 6 的气泡复用）
  - CSS 类：`.mm-note-badge`、`.mm-note-svg`、`.mm-note-box`、`.mm-note-lines`

> 设计文档写的 key 名是 `toolbar.note.disabled.root`，这里改用 `toolbar.note.blockedRoot`，与既有的 `toolbar.marks.blockedRoot` / `toolbar.remove.blockedRoot` 保持一致。

- [ ] **Step 1: 在 `src/i18n.ts` 的中文表加 5 个 key**

`"badge.note"` 放在 `"badge.collapsed"` 之前（与其它 badge 相邻）：

```ts
  "badge.note": "备注",
```

其余三个工具栏/浮层相关的，放在 `"toolbar.link"` 之前：

```ts
  "toolbar.note": "备注",
  "toolbar.note.blockedRoot": "根节点不能带备注：文件里没有一级标题时无处写回",
  "note.placeholder": "写点备注……",
  "note.hint": "Cmd/Ctrl + Enter 保存，Esc 取消",
```

- [ ] **Step 2: 在英文表加对应的 5 条**

位置与中文表一一对应：

```ts
  "badge.note": "Note",
```

```ts
  "toolbar.note": "Note",
  "toolbar.note.blockedRoot":
    "The root node can't carry a note: there's nowhere to write it back when the file has no level-1 heading",
  "note.placeholder": "Write a note…",
  "note.hint": "Cmd/Ctrl + Enter to save, Esc to cancel",
```

- [ ] **Step 3: 确认漏译是编译错误这条门禁生效**

Run: `npm run typecheck`
Expected: PASS。临时删掉英文表里任意一条新 key 再跑一次，应当 FAIL（确认门禁没被绕过），然后加回来。

- [ ] **Step 4: 在 `src/view/node-el.ts` 加 `buildNoteBadge`**

放在 `buildFlagBadge` 之后：

```ts
/**
 * 备注角标。
 *
 * **刻意不设 `.title`**：其余角标用原生 title 做提示，而备注有自己的悬浮气泡
 * （note-tip.ts），再挂一个原生 tooltip 会有两个浮层抢同一块位置。可访问名
 * 只走 aria-label。
 */
export function buildNoteBadge(parent: HTMLElement): HTMLElement {
  const wrap = el("span", "mm-mark mm-note-badge", parent);
  wrap.setAttribute("aria-label", t("badge.note"));

  const svg = svgEl("svg", "mm-note-svg", wrap);
  svg.setAttribute("viewBox", "0 0 16 16");

  const box = svgEl("rect", "mm-note-box", svg);
  box.setAttribute("x", "2.5");
  box.setAttribute("y", "2.5");
  box.setAttribute("width", "11");
  box.setAttribute("height", "11");
  box.setAttribute("rx", "2");

  const lines = svgEl("path", "mm-note-lines", svg);
  lines.setAttribute("d", "M 5.5 6.5 H 10.5 M 5.5 9.5 H 9");

  return wrap;
}
```

- [ ] **Step 5: 让 `buildMarks` 也渲染备注角标**

把 `buildMarks` 的签名从 `(marks: Marks, parent: HTMLElement)` 改成 `(node: MindNode, parent: HTMLElement)`：

```ts
function buildMarks(node: MindNode, parent: HTMLElement): void {
  const { marks } = node;
  const hasNote = node.note !== undefined;
  if (
    marks.priority === undefined &&
    marks.progress === undefined &&
    marks.flag === undefined &&
    !hasNote
  ) {
    return;
  }
  const wrap = el("span", "mm-marks", parent);
  if (marks.priority !== undefined) buildPriorityBadge(marks.priority, wrap);
  if (marks.progress !== undefined) buildProgressBadge(marks.progress, wrap);
  if (marks.flag !== undefined) buildFlagBadge(marks.flag, wrap);
  if (hasNote) buildNoteBadge(wrap);
}
```

在 `buildNodeEl` 里把调用点改成 `buildMarks(node, element);`。

改完之后 `Marks` 类型在 `node-el.ts` 里就没有使用点了，必须从顶部的
`import { isHeading, type FlagColor, type Marks, type MindNode } from "../model/types";`
里删掉 `type Marks,`。`tsconfig.json` 开着 `noUnusedLocals`，不删是编译错误——
下一步的 `npm run typecheck` 会直接告诉你。

- [ ] **Step 6: 导出 `renderInline` 供 Task 6 复用**

把 `function renderInline(` 改为 `export function renderInline(`，并在它的文档注释末尾补一句：

```
 * 备注气泡（note-tip.ts）复用这个函数渲染备注正文，同样不走 innerHTML。
```

- [ ] **Step 7: 在 `styles.css` 加角标样式**

在「标记」小节里，`.mm-progress-svg, .mm-flag-svg { … }` 规则**之后**加：

```css
.mm-note-badge {
  color: var(--text-muted);
}

.mm-note-svg {
  width: 16px;
  height: 16px;
}

.mm-note-box,
.mm-note-lines {
  fill: none;
  stroke: currentColor;
  stroke-width: 1.4;
  stroke-linecap: round;
}
```

- [ ] **Step 8: 跑门禁**

Run: `npm run typecheck && npm test && npm run build && npm run check:i18n`
Expected: 全绿。`check:i18n` 必须通过——新加的角标文案走的是 `t("badge.note")`，代码里没有中文字面量。

- [ ] **Step 9: 提交**

```bash
git add src/i18n.ts src/view/node-el.ts styles.css
git commit -m "feat: 节点上的备注角标与中英文案"
```

---

### Task 6: 悬浮气泡

**Files:**
- Create: `src/view/note-tip.ts`
- Modify: `src/view.ts`
- Modify: `styles.css`

**Interfaces:**
- Consumes: `buildNoteBadge` 产出的 `.mm-note-badge`（Task 5）、`renderInline`（Task 5 导出）、`parseInline`、`placeNear`、`OnOpenLink`
- Produces: `attachNoteTips(host: NoteTipHost): { close(): void }`，其中

```ts
export interface NoteTipHost {
  root: HTMLElement;
  on<K extends keyof HTMLElementEventMap>(
    type: K,
    handler: (event: HTMLElementEventMap[K]) => void,
  ): void;
  /** 按节点 id 取备注正文；没有备注返回 null */
  noteOf(id: string): string | null;
  onOpenLink?: OnOpenLink;
}
```

- [ ] **Step 1: 写 `src/view/note-tip.ts`**

```ts
import { parseInline } from "../model/inline";
import { el } from "./dom";
import { renderInline, type OnOpenLink } from "./node-el";
import { placeNear } from "./popover";

export interface NoteTipHost {
  root: HTMLElement;
  on<K extends keyof HTMLElementEventMap>(
    type: K,
    handler: (event: HTMLElementEventMap[K]) => void,
  ): void;
  /** 按节点 id 取备注正文；没有备注返回 null */
  noteOf(id: string): string | null;
  onOpenLink?: OnOpenLink;
}

/** 悬浮多久才弹出。太短会在鼠标扫过密集导图时连环闪烁。 */
const SHOW_DELAY_MS = 200;
/** 移开多久才收起。留出把鼠标移进气泡里选文字 / 点链接的时间。 */
const HIDE_DELAY_MS = 120;

/**
 * `Element` 而不是 `HTMLElement`：角标是内联 SVG，鼠标停在图形上时
 * event.target 是 SVGElement——它有 closest()，但不是 HTMLElement。按
 * HTMLElement 收窄会让这条守卫整个失效，气泡永远不弹（AGENTS.md 第 7 条）。
 */
function badgeFrom(target: EventTarget | null): HTMLElement | null {
  if (!(target instanceof Element)) return null;
  return target.closest<HTMLElement>(".mm-note-badge");
}

function nodeIdFrom(element: Element): string | null {
  return element.closest<HTMLElement>(".mm-node")?.dataset.id ?? null;
}

/**
 * 给备注角标接上悬浮气泡。
 *
 * 用 pointerover/pointerout 的**事件委托**而不是给每个角标挂 pointerenter：
 * 角标每次 render() 都会被重建，逐个挂监听既要管解绑、又会在重渲染时漏掉；
 * 委托到 this.root 上则天然覆盖所有当前与未来的角标。注册必须走调用方的
 * `on`（即 view.ts 的 registerDomEvent），并且只在 eventsAttached 块里调用一次
 * ——重复注册会堆叠监听器（AGENTS.md 第 6 条）。
 */
export function attachNoteTips(host: NoteTipHost): { close(): void } {
  let tip: HTMLElement | null = null;
  let ownerId: string | null = null;
  let showTimer: number | null = null;
  let hideTimer: number | null = null;

  function clearShowTimer(): void {
    if (showTimer !== null) window.clearTimeout(showTimer);
    showTimer = null;
  }

  function clearHideTimer(): void {
    if (hideTimer !== null) window.clearTimeout(hideTimer);
    hideTimer = null;
  }

  function close(): void {
    clearShowTimer();
    clearHideTimer();
    tip?.remove();
    tip = null;
    ownerId = null;
  }

  function open(id: string, badge: HTMLElement, text: string): void {
    tip?.remove();
    // mm-no-pan：浮在画布之上的元素都要带这个类，否则在气泡上按下拖动会连带
    // 平移底下的画布（AGENTS.md 第 7 条）。
    const wrap = el("div", "mm-note-tip mm-no-pan", host.root);
    for (const line of text.split("\n")) {
      const row = el("div", "mm-note-line", wrap);
      if (line === "") {
        // 空行必须占一行高度，否则段落间距会塌陷成 0。
        row.textContent = " ";
        continue;
      }
      renderInline(parseInline(line), row, host.onOpenLink);
    }
    placeNear(wrap, badge.getBoundingClientRect(), host.root.getBoundingClientRect());
    tip = wrap;
    ownerId = id;
  }

  host.on("pointerover", (event: PointerEvent) => {
    // 鼠标进到气泡自己身上：取消收起，让用户能选文字、点链接。
    if (tip !== null && event.target instanceof Node && tip.contains(event.target)) {
      clearHideTimer();
      return;
    }

    const badge = badgeFrom(event.target);
    if (badge === null) return;
    const id = nodeIdFrom(badge);
    if (id === null) return;
    const text = host.noteOf(id);
    if (text === null) return;

    clearHideTimer();
    // 已经为这个节点开着了：不重开，避免鼠标在角标内部移动时气泡反复重建。
    if (tip !== null && ownerId === id) return;

    clearShowTimer();
    showTimer = window.setTimeout(() => {
      showTimer = null;
      open(id, badge, text);
    }, SHOW_DELAY_MS);
  });

  host.on("pointerout", (event: PointerEvent) => {
    const leavingBadge = badgeFrom(event.target) !== null;
    const leavingTip =
      tip !== null && event.target instanceof Node && tip.contains(event.target);
    if (!leavingBadge && !leavingTip) return;

    // 在角标内部（span → svg → path）之间移动同样派发 pointerout，
    // relatedTarget 仍在角标或气泡里时不该收起。
    const to = event.relatedTarget;
    if (to instanceof Node) {
      if (badgeFrom(to) !== null) return;
      if (tip !== null && tip.contains(to)) return;
    }

    clearShowTimer();
    clearHideTimer();
    hideTimer = window.setTimeout(() => {
      hideTimer = null;
      close();
    }, HIDE_DELAY_MS);
  });

  return { close };
}
```

- [ ] **Step 2: 在 `styles.css` 加气泡样式**

在 `.mm-input-popover` 规则之前加：

```css
.mm-note-tip {
  position: absolute;
  z-index: 25;
  max-width: 320px;
  padding: 8px 10px;
  font-size: var(--font-ui-small);
  line-height: 1.5;
  background: var(--background-secondary);
  border: 1px solid var(--background-modifier-border);
  border-radius: 8px;
  box-shadow: var(--shadow-s);
  overflow-wrap: anywhere;
}

.mm-note-line {
  white-space: pre-wrap;
}
```

`z-index: 25` 低于编辑浮层的 30：气泡是被动展示，编辑浮层是主动操作，两者重叠时后者在上。

- [ ] **Step 3: 在 `src/view.ts` 里加字段与 import**

import 区加：

```ts
import { attachNoteTips } from "./view/note-tip";
```

字段区，在 `dragControl` 之后加：

```ts
  /** 备注悬浮气泡的句柄。与 dragControl 一样只挂一次监听（在 eventsAttached
   *  块里），因此在重置状态时只 close() 不置 null——置 null 会让下一次 render()
   *  以为还没挂过，但监听器实际上还在，形成堆叠（AGENTS.md 第 6 条）。 */
  private noteTips: { close(): void } | null = null;
```

- [ ] **Step 4: 在 `eventsAttached` 块里挂上**

把

```ts
      if (!this.eventsAttached) {
        this.attachCameraEvents();
        this.attachInteractionLayer();
        this.attachDragLayer();
        this.eventsAttached = true;
      }
```

改成

```ts
      if (!this.eventsAttached) {
        this.attachCameraEvents();
        this.attachInteractionLayer();
        this.attachDragLayer();
        this.attachNoteTipLayer();
        this.eventsAttached = true;
      }
```

并在 `attachDragLayer()` 方法之后加：

```ts
  private attachNoteTipLayer(): void {
    this.noteTips = attachNoteTips({
      root: this.root,
      on: (type, handler) => this.registerDomEvent(this.root, type, handler),
      noteOf: (id) => {
        if (this.doc === null) return null;
        return findNode(this.doc.root, id)?.note?.text ?? null;
      },
      onOpenLink: (target, event) => this.openLink(target, event),
    });
  }
```

- [ ] **Step 5: 在 `render()` 开头关掉气泡**

`render()` 的第一行改成：

```ts
  private render(): void {
    // 气泡锚定在一个马上要被重建的角标上，锚点消失后 pointerout 不会再派发，
    // 不主动关就会留一个孤儿浮层挂在画布上。气泡是悬浮触发的、不绑定选中，
    // 所以不走 closeStaleOverlays（那条路径判的是 overlayOwnerId === selectedId）。
    this.noteTips?.close();
    if (this.parseError !== null) {
```

- [ ] **Step 6: 在 `resetDocBoundState()` 里关掉气泡**

在 `this.dragControl?.cancel();` 之前加：

```ts
    this.noteTips?.close();
```

- [ ] **Step 7: 跑门禁**

Run: `npm run typecheck && npm test && npm run build && npm run check:purity && npm run check:i18n`
Expected: 全绿。

- [ ] **Step 8: 提交**

```bash
git add src/view/note-tip.ts src/view.ts styles.css
git commit -m "feat: 悬浮备注角标弹出气泡"
```

- [ ] **Step 9: 在真实 Obsidian 里人工确认**

这一步没有自动化替代（`src/view/` 没有自动化测试）。把 `main.js` / `manifest.json` / `styles.css` 放进测试 vault 的插件目录，用一个带备注的文件验：

1. 带备注的节点右侧出现角标，不带备注的没有。
2. 鼠标停在角标上约 0.2 秒后弹出气泡，移开约 0.12 秒后收起。
3. 鼠标从角标移进气泡，气泡**不消失**；从气泡移开才消失。
4. 气泡里的 `**粗体**` / `[[链接]]` 渲染成粗体与链接，点链接能跳转。
5. 在气泡上按下并拖动，画布**不**跟着平移。
6. 气泡弹出时按方向键切换选中（触发 render），气泡消失，不残留。

报告里必须把这 6 条标为「人工验证」，不要声称视觉或交互行为正确却没实际验过。

---

### Task 7: 编辑浮层与工具栏按钮

**Files:**
- Create: `src/view/note-popover.ts`
- Modify: `src/view/toolbar.ts`
- Modify: `src/view.ts`
- Modify: `styles.css`

**Interfaces:**
- Consumes: `setNote` / `canNote`（Task 4）、i18n key（Task 5）、`placeNear`
- Produces:
  - `openNotePopover(host, anchor, options): { close(): void }`，其中 `options: { initial: string; onSubmit(value: string): void; onClose?(): void }`
  - `NodeCapabilities.canNote: boolean`
  - `ToolbarHandlers.onNote(anchor: DOMRect): void`

- [ ] **Step 1: 写 `src/view/note-popover.ts`**

```ts
import { t } from "../i18n";
import { el } from "./dom";
import { placeNear } from "./popover";

/**
 * 多行备注的编辑浮层。Enter 换行，Cmd/Ctrl+Enter 提交，Esc 取消，点击外部提交。
 *
 * 不复用 input-popover.ts：那是单行 `<input>` 且 Enter 即提交，键盘语义与多行
 * 输入正相反，合并会让两边都变糊。
 */
export function openNotePopover(
  host: HTMLElement,
  anchor: DOMRect,
  options: {
    initial: string;
    onSubmit(value: string): void;
    /** 浮层以任何方式关闭时调用一次，供调用方清空自己持有的引用
     *  （与 openMarksPanel / openInputPopover 同款）。 */
    onClose?(): void;
  },
): { close(): void } {
  // mm-no-pan：画布上「界面元素」的通用标记，否则在浮层上按下并拖动会被画布
  // 当成平移手势处理（AGENTS.md 第 7 条）。
  const wrap = el("div", "mm-note-popover mm-no-pan", host);
  const area = el("textarea", "mm-note-input", wrap);
  area.placeholder = t("note.placeholder");
  area.value = options.initial;
  const hint = el("div", "mm-note-hint", wrap);
  hint.textContent = t("note.hint");

  placeNear(wrap, anchor, host.getBoundingClientRect());

  let closed = false;
  let submitted = false;

  function close(): void {
    if (closed) return;
    closed = true;
    document.removeEventListener("pointerdown", onDocPointerDown, true);
    wrap.remove();
    options.onClose?.();
  }

  function submit(): void {
    if (submitted || closed) return;
    submitted = true;
    // 只去掉尾部空白：留着会让每次编辑都在文件里多写一行孤零零的 `>`。
    // 前导空白是用户可能真想要的缩进，保留。
    const value = area.value.replace(/[ \t\n]+$/, "");
    close();
    options.onSubmit(value);
  }

  function onDocPointerDown(event: PointerEvent): void {
    if (event.target instanceof Node && wrap.contains(event.target)) return;
    submit();
  }

  area.addEventListener("keydown", (event) => {
    if (event.key === "Escape") {
      event.preventDefault();
      // 只关浮层，不让 Esc 冒泡到画布的 keydown 把当前选中也清空
      // ——用户此刻的意图只是收起浮层（与 marks-panel.ts 同款）。
      event.stopPropagation();
      close();
      return;
    }

    if (event.key === "Enter" && (event.metaKey || event.ctrlKey)) {
      event.preventDefault();
      event.stopPropagation();
      submit();
      return;
    }

    // 带修饰键：放行给 Obsidian 全局热键。stopPropagation 会对所有祖先生效，
    // 包括挂着全局热键的 document（AGENTS.md 第 6 条）。
    if (event.metaKey || event.ctrlKey || event.altKey) return;

    // 其余按键在 textarea 内消费掉，绝不能冒泡到 this.root 的 keydown：
    // 那里 Enter 会新建兄弟节点、方向键会切换选中，用户打字就会同时改变导图。
    event.stopPropagation();
  });

  document.addEventListener("pointerdown", onDocPointerDown, true);
  area.focus();
  // 光标落到末尾，方便直接续写已有备注。
  area.setSelectionRange(area.value.length, area.value.length);

  return { close };
}
```

- [ ] **Step 2: 在 `styles.css` 加浮层样式**

在 `.mm-input-popover .mm-input { … }` 之后加：

```css
.mm-note-popover {
  position: absolute;
  z-index: 30;
  display: flex;
  flex-direction: column;
  gap: 4px;
  padding: 6px;
  background: var(--background-primary);
  border: 1px solid var(--background-modifier-border);
  border-radius: 8px;
  box-shadow: var(--shadow-s);
}

.mm-note-popover .mm-note-input {
  width: 280px;
  min-height: 96px;
  font-family: inherit;
  resize: vertical;
}

.mm-note-hint {
  font-size: var(--font-ui-smaller);
  color: var(--text-muted);
}
```

- [ ] **Step 3: 在 `src/view/toolbar.ts` 里加能力位与处理器**

`ToolbarHandlers` 加一行（放在 `onLink` 之后）：

```ts
  onNote(anchor: DOMRect): void;
```

`NodeCapabilities` 加一行（放在 `canMark` 之后）：

```ts
  canNote: boolean;
```

- [ ] **Step 4: 加「备注」按钮**

在 `specs` 数组里，`icon: "link"` 那一项**之后**插入：

```ts
    {
      icon: "sticky-note",
      label: t("toolbar.note"),
      action: (button) => handlers.onNote(button.getBoundingClientRect()),
      // 根节点不能带备注：parse 不对根做 splitNote、setNote 对根 id 是 no-op
      // （见 model/tree-ops.ts）。不在这里禁用的话，选中根节点打开浮层后写什么
      // 都会静默失败。判断只从 tree-ops.canNote 来，视图层不重新推导。
      disabled: (caps) => !caps.canNote,
      disabledLabel: () => t("toolbar.note.blockedRoot"),
    },
```

- [ ] **Step 5: 在 `src/view.ts` 里接线**

import 区加：

```ts
import { openNotePopover } from "./view/note-popover";
```

并把 `from "./model/tree-ops"` 的 import 列表补上 `canNote` 与 `setNote`。

字段区，在 `inputPopover` 之后加：

```ts
  /** 备注编辑浮层。与 marksPanel/inputPopover 同款：它在 document 上挂着
   *  pointerdown 捕获监听，DOM 被 clear(this.root) 销毁不会解绑，必须显式 close()。 */
  private notePopover: { close(): void } | null = null;
```

- [ ] **Step 6: 加 `openNote` 方法**

在 `insertLink` 之后加：

```ts
  private openNote(id: string, anchor: DOMRect): void {
    if (this.doc === null) return;
    const node = findNode(this.doc.root, id);
    if (node === null) return;

    this.notePopover?.close();
    this.overlayOwnerId = id;
    this.notePopover = openNotePopover(this.root, anchor, {
      initial: node.note?.text ?? "",
      onSubmit: (value) => {
        if (this.doc === null) return;
        this.applyDoc({ ...this.doc, root: setNote(this.doc.root, id, value) });
      },
      onClose: () => {
        this.notePopover = null;
      },
    });
  }
```

- [ ] **Step 7: 把按钮接到 `createToolbarForView`**

在 `onLink: …` 之后加：

```ts
      onNote: (anchor) => this.withSelection((id) => this.openNote(id, anchor)),
```

- [ ] **Step 8: 把 `canNote` 接到能力位**

在 `syncToolbar()` 的 `this.toolbar.showFor(element, { … })` 里，`canMark` 之后加：

```ts
      canNote: canNote(this.doc.root, node.id),
```

- [ ] **Step 9: 把浮层接进两处生命周期清理**

`closeStaleOverlays()` 改成：

```ts
  private closeStaleOverlays(): void {
    if (
      this.marksPanel === null &&
      this.inputPopover === null &&
      this.notePopover === null
    ) {
      return;
    }
    if (this.overlayOwnerId === this.selectedId && this.editingId === null) return;
    this.marksPanel?.close();
    this.inputPopover?.close();
    this.notePopover?.close();
    this.marksPanel = null;
    this.inputPopover = null;
    this.notePopover = null;
    this.overlayOwnerId = null;
  }
```

`resetDocBoundState()` 里，在 `this.inputPopover = null;` 之后加：

```ts
    this.notePopover?.close();
    this.notePopover = null;
```

- [ ] **Step 10: 跑门禁**

Run: `npm run typecheck && npm test && npm run build && npm run check:purity && npm run check:i18n`
Expected: 全绿。`typecheck` 会强制你补齐 `NodeCapabilities.canNote` 与 `ToolbarHandlers.onNote` 的所有实现点——没补会是编译错误。

- [ ] **Step 11: 提交**

```bash
git add src/view/note-popover.ts src/view/toolbar.ts src/view.ts styles.css
git commit -m "feat: 工具栏备注按钮与多行编辑浮层"
```

- [ ] **Step 12: 在真实 Obsidian 里人工确认**

1. 选中一个列表项，工具栏出现「备注」按钮（便签图标，**不是空白方块**——若图标空白说明 `sticky-note` 不在这个 Obsidian 版本的 lucide 集合里，换 `message-square`）。
2. 选中根节点时该按钮置灰，悬浮显示禁用原因。
3. 点按钮弹出输入框，写两行文字，Cmd/Ctrl+Enter 保存；节点上出现角标，悬浮能看到两行。
4. 切到源码视图，确认文件里是节点行下方缩进一层的 `> 第一行` / `> 第二行`，且**其余部分零 diff**（`git diff` 只应有这两行）。
5. 再打开输入框，全选删空并保存，角标消失，文件里那两行没了。
6. 在输入框里按 Enter 换行——**不能**新建兄弟节点；按方向键——**不能**切换选中节点。
7. 按 Esc 取消，原备注不变。
8. 把一个带备注的节点拖到另一层深度，切到源码视图确认备注行的缩进跟着变了、仍在列表项内部。

同样，这 8 条在报告里必须标为「人工验证」。

---

### Task 8: 文档

**Files:**
- Modify: `AGENTS.md`
- Modify: `README.md`、`README.zh-CN.md`
- Modify: `docs/MANUAL-VERIFICATION.md`

**Interfaces:**
- Consumes: 前 7 个任务的全部行为
- Produces: 无代码接口。

- [ ] **Step 1: 在 `AGENTS.md` 加一条硬约束**

在第 12 条（节点下划线）**之后**、「## 门禁」之前加：

```markdown
### 13. 节点备注是引用块，`Note.raw` 不能省

备注是**紧跟节点行的连续引用行**（`> …`），解析期由 `src/model/note.ts` 的
`splitNote` 从 `continuation` 里摘进 `MindNode.note`，序列化期由 `serializeNote`
写回。选引用块而不是 HTML 注释或 frontmatter 映射表，核心理由是 **`>` 前缀天然
是逃逸字符**：备注正文里的 `- x` / `# x` / 代码围栏被写成 `> - x` / `> # x` /
`> ``` ` 之后，三条节点行正则（顶格 `^#`、`^[ \t]*[-*+][ \t]+`、`^[ \t]*` 后接
围栏字符）全都不匹配，用户写什么都不可能长出假节点或打开假围栏。回归测试在
`tests/note.test.ts` 的「备注正文不改变导图结构」里，用 fast-check 生成敌意行。

**`Note.raw` 不能省。** 用户写的可能是 `>备注`（无空格）、`>  备注`（两个空格）、
tab 缩进。只存解码后的 `text`、写回时统一渲染成 `> `，就是第六条未获许可的写回
归一化。解析期把原始行整条留住，只有用户真正编辑过备注才置 `raw = null`，那时
才由 `serialize` 按节点当前的缩进重新生成。

**`moveNode` 必须清掉移动子树内所有 `note.raw`**：深度变了旧缩进不再对，
留着会让备注行掉出列表外（仍能解析回同一个节点，但 Obsidian 自己的阅读视图
里位置是错的）。

**根节点永不带备注**，与第 8 条对标记的规则一致：`setNote` 对根 id 是 no-op，
`parse` 对称地不对根做 `splitNote`，工具栏按钮在选中根节点时禁用。

**备注不在 `continuation` 里，所以 `hasHiddenContent` 看不见它**——加备注不会
让节点变成不可删、不可拖。这是刻意的：备注在导图上是可见的（角标 + 气泡），
不属于「图上不可见的正文」。不要把备注行留在 `continuation` 里「省事」，
那会静默禁用删除与拖拽两个按钮。
```

- [ ] **Step 2: 更新 `README.zh-CN.md`**

在「## 与其他思维导图插件的区别」的要点列表里，`- **支持优先级 / 进度 / 旗帜标记**…` 之后加一条：

```markdown
- **节点可以带备注**，悬浮角标即可查看；备注存成节点下方的 Markdown 引用块，不装插件也读得到
```

在「## 数据格式」一节的末尾加一小段：

```markdown
### 备注

紧跟节点行的连续引用行就是这个节点的备注：

```markdown
- 三季度目标
  > 等 A 确认口径。
  > 参考上季度的留存拆分。
  - 拉新
```

导图上显示成一个角标，鼠标悬浮弹出。它是普通 Markdown 引用块——阅读视图里正常
渲染，能被搜索到，不装这个插件也读得写得。中间隔了其它内容之后的引用块不算备注。
根节点不支持备注。
```

- [ ] **Step 3: 同步 `README.md`（英文）**

在英文 README 的对应位置加同义的两处内容：

要点列表：

```markdown
- **Nodes can carry notes**, shown on hover; a note is stored as a plain Markdown blockquote under the node, readable without this plugin
```

数据格式一节：

```markdown
### Notes

A run of blockquote lines directly under a node line is that node's note:

```markdown
- Q3 goals
  > Waiting on A to confirm the definition.
  > See last quarter's retention breakdown.
  - Acquisition
```

The mindmap shows a badge; hovering it pops the note up. It is an ordinary
Markdown blockquote — it renders in reading view, it is searchable, and it stays
readable and editable without this plugin. A blockquote separated from the node
line by other content is not a note. The root node does not support notes.
```

- [ ] **Step 4: 在 `docs/MANUAL-VERIFICATION.md` 加一节**

在「## 9c. 界面语言」之后加：

```markdown
## 9d. 节点备注

自动化门禁覆盖了备注的解析、写回、往返字节、`>` 转义与 tree-ops 的编辑语义；
**角标、气泡、编辑浮层的一切视觉与交互行为都没有自动化测试**，只能人工验。

### 展示

1. 打开一个带 `> 备注` 的文件：带备注的节点右侧出现便签角标，其它节点没有。
2. 鼠标停在角标上约 0.2 秒弹出气泡；移开约 0.12 秒收起。
3. 鼠标从角标**移进气泡**，气泡不消失；从气泡移开才消失。
4. 气泡里的 `**粗体**` / `[[链接]]` 正常渲染，点链接能跳转。
5. 在气泡上按下并拖动，画布不跟着平移。
6. 气泡弹出时按方向键切换选中，气泡消失且不残留孤儿浮层。

### 编辑

7. 选中列表项，工具栏「备注」按钮是便签图标而不是空白方块。
8. 选中根节点时该按钮置灰，悬浮给出禁用原因。
9. 写两行备注、Cmd/Ctrl+Enter 保存：角标出现，气泡能看到两行。
10. `git diff` 只有那两行 `>`，缩进比节点行深一层，文件其余部分零字节差异。
11. 输入框里按 Enter 换行**不会**新建兄弟节点；按方向键**不会**切换选中。
12. Esc 取消不改动原备注；清空保存则角标与文件里的引用行一起消失。

### 边界

13. 把带备注的节点拖到另一层深度，源文件里备注行的缩进跟着变，仍在列表项内部。
14. 只带备注（没有其它续行）的节点**可以删除、可以拖拽**——备注不算「图上不可见的正文」。
15. 备注正文里写 `- 列表`、`# 标题`、代码围栏，保存后导图节点数不变、不出现假节点。
16. 根节点 H1 下面手写的引用块不会被当成备注（没有角标），且字节保持不变。
```

- [ ] **Step 5: 跑门禁**

Run: `npm run typecheck && npm test && npm run build && npm run check:purity && npm run check:i18n`
Expected: 全绿（文档改动不影响，但确认没有顺手改坏代码）。

- [ ] **Step 6: 提交**

```bash
git add AGENTS.md README.md README.zh-CN.md docs/MANUAL-VERIFICATION.md
git commit -m "docs: 节点备注的约束、功能说明与人工验证清单"
```

---

## 完成判据

- 门禁五条全绿：`npm run typecheck` / `npm test` / `npm run build` / `npm run check:purity` / `npm run check:i18n`。
- `tests/roundtrip.test.ts` 的 `CORPUS` 里 11 条备注语料逐字节相等。
- `tests/note.test.ts` 的对抗性属性测试（500 次运行）通过。
- Task 6 Step 9 与 Task 7 Step 12 的 14 条人工验证在真实 Obsidian 里逐条走过，报告里明确区分「机械验证过的」与「人工确认过的」，**不要声称视觉或交互行为正确却没实际验过**。
