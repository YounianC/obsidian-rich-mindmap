# 界面文字 i18n Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 插件的 48 处面向用户字符串支持中文与英文，语言在设置页三选（跟随 Obsidian / 中文 / English），默认跟随。

**Architecture:** 新增 `src/i18n.ts`：`zh` 字典推导出 key 联合类型，`en` 声明为 `Record<MessageKey, string>`（漏译即编译错误）；`t(key, params?)` 读模块级当前语言；`resolveLocale(setting, appLanguage)` 是纯函数。`getLanguage()` 只在 `main.ts` 一处调用。`main.ts` 的 `applyLanguage()` 负责设语言、重注册命令、通知各导图视图重建图层。新增第五条门禁 `scripts/check-i18n.mjs` 扫描视图与主层字符串字面量里的中文。

**Tech Stack:** TypeScript strict、Vitest、esbuild、Obsidian API（`getLanguage` @since 1.8.7、`removeCommand` @since 1.7.2、`setControlValue`/`update` @since 1.13.0）。无新依赖。

设计文档：[docs/superpowers/specs/2026-09-04-i18n-design.md](../specs/2026-09-04-i18n-design.md)。冲突时先改文档再改计划。

## 关于 TDD 的偏离（已获人类授权）

人类在本会话早前明确要求「不要使用 TDD 流程，直接进行实现，最终进行一次测试」。本计划据此分开处理：

- **Task 1（i18n 模块）与 Task 6（门禁脚本）走 TDD**：这两处有真实逻辑（语言解析、插值、注释剥离），先写失败测试有实际价值。
- **Task 2–5、7（字符串搬迁）不走 TDD**：把字面量换成 `t("key")` 是机械转写，先写失败测试只是形式。改完跑门禁验证。

## Global Constraints

以下每条来自 `AGENTS.md` 或设计文档，适用于**每一个** task：

- **纯函数层边界**：`src/model/**` 加 `src/view/layout.ts`、`src/view/camera.ts` 不得 `import obsidian`，不得出现 `document` / `window` / `HTMLElement`。`npm run check:purity` 强制校验。
- **`src/i18n.ts` 不得 `import obsidian`**，也**不得放进 `src/model/`**。前者让字典与 `resolveLocale` 可以直接单测；后者因为 `t()` 有模块级可变状态、不是纯函数，放进纯函数层是在骗人。
- **写回归一化仍然只有五条**，本改动不得新增任何一条。唯一碰到文件内容的地方是新建文件的默认名（Task 2），那是新建文件，不改写已有内容。
- **`tests/roundtrip.test.ts` 的 `CORPUS` 只能加不能放宽。** 本改动不应需要动它。
- **根节点永不携带标记**（AGENTS.md 第 8 条），本改动不碰这条逻辑。
- **视图重建只能走 `this.layers = null` + `render()`**（AGENTS.md 第 6 条：`render()` 的重建分支靠 `eventsAttached` 守卫防监听器堆叠，另写一套会让一个滚轮档位变成 `factor^N` 倍缩放）。
- TypeScript `strict: true`，**不用 `any`**（必要时 `unknown` + 类型守卫）。
- 代码注释用中文，跟着周边风格（解释「为什么」而非「做什么」）。**注释不受 i18n 约束，只有面向用户的字符串受约束。**
- **每个 task 结束前门禁全绿**：`npm run typecheck`、`npm test`、`npm run build`、`npm run check:purity`。Task 6 之后再加 `npm run check:i18n`。
- 涉及 `src/view/` 或 `styles.css` 的改动，报告里必须区分「机械验证过的」与「需要人在真实 Obsidian 里确认的」，**不得声称视觉或交互行为正确**。

## File Structure

| 文件 | 职责 | 动作 |
|---|---|---|
| `src/i18n.ts` | 字典、`Locale`、`resolveLocale`、`setLocale`、`t` | 新建 |
| `scripts/check-i18n.mjs` | 第五条门禁：扫视图/主层字面量里的中文 | 新建 |
| `scripts/check-i18n.d.mts` | 上面那个脚本的手写类型声明（tsconfig 未开 allowJs） | 新建 |
| `tests/i18n.test.ts` | `resolveLocale` / `t` / 字典完整性 / 注释剥离 | 新建 |
| `src/model/new-file.ts` | `basename` 改为必需参数 | 改 |
| `src/view.ts` | 7 个 key + `refreshLocale()` | 改 |
| `src/view/toolbar.ts` | 15 处 | 改 |
| `src/view/marks-panel.ts` | 6 处 | 改 |
| `src/view/node-el.ts` | 4 处 | 改 |
| `src/view/controls.ts` | 3 处 | 改 |
| `src/main.ts` | 7 处 + `applyLanguage()` + 命令重注册 | 改 |
| `src/settings.ts` | 语言设置项（声明式 + 兜底两条路径）、`setControlValue` 钩子 | 改 |

不改：`parser.ts`、`serializer.ts`、`tree-ops.ts`、`types.ts`、`marks.ts`、`inline.ts`、`collapse-state.ts`、`layout.ts`、`camera.ts`、`drag.ts`、`interaction.ts`、`renderer.ts`、`measure.ts`、`popover.ts`、`input-popover.ts`、`dom.ts`、`styles.css`。

## Task 顺序的依赖

Task 1 必须最先（其余都 `import { t }`）。Task 6 必须最后（门禁在字符串搬完前必然失败）。Task 5 依赖 Task 3 提供的 `MindmapView.refreshLocale()`。

---

### Task 1: `src/i18n.ts` — 字典、语言解析、`t()`

**Files:**
- Create: `src/i18n.ts`
- Create: `tests/i18n.test.ts`

**Interfaces:**
- Consumes: 无
- Produces:
  - `type Locale = "zh" | "en"`
  - `type LanguageSetting = "auto" | "zh" | "en"`
  - `type MessageKey`（由 `zh` 表推导）
  - `resolveLocale(setting: LanguageSetting, appLanguage: string): Locale`
  - `setLocale(locale: Locale): void`
  - `t(key: MessageKey, params?: Record<string, string | number>): string`
  - `LOCALES: readonly Locale[]`、`MESSAGES: Record<Locale, Record<MessageKey, string>>`（均供测试遍历，生产代码用 `t()`）

- [ ] **Step 1: 写失败测试**

创建 `tests/i18n.test.ts`：

```ts
import { beforeEach, describe, expect, it } from "vitest";
import {
  LOCALES,
  MESSAGES,
  resolveLocale,
  setLocale,
  t,
  type MessageKey,
} from "../src/i18n";

describe("resolveLocale", () => {
  it("显式取值直接生效，忽略 Obsidian 的语言", () => {
    expect(resolveLocale("zh", "en")).toBe("zh");
    expect(resolveLocale("en", "zh")).toBe("en");
  });

  it("auto 跟随 Obsidian：zh 前缀归中文", () => {
    expect(resolveLocale("auto", "zh")).toBe("zh");
    expect(resolveLocale("auto", "zh-TW")).toBe("zh");
    expect(resolveLocale("auto", "zh-HK")).toBe("zh");
  });

  it("auto 跟随 Obsidian：其余一律英文", () => {
    expect(resolveLocale("auto", "en")).toBe("en");
    expect(resolveLocale("auto", "ja")).toBe("en");
    expect(resolveLocale("auto", "")).toBe("en");
  });
});

describe("字典完整性", () => {
  it("两表 key 集合完全相同", () => {
    const zhKeys = Object.keys(MESSAGES.zh).sort();
    const enKeys = Object.keys(MESSAGES.en).sort();
    expect(enKeys).toEqual(zhKeys);
  });

  it("没有空串值——类型系统挡不住 \"\"", () => {
    for (const locale of LOCALES) {
      for (const [key, value] of Object.entries(MESSAGES[locale])) {
        expect(value, `${locale}.${key} 为空`).not.toBe("");
      }
    }
  });

  it("两表的插值占位符集合一致，漏一个参数就是漏一段文案", () => {
    const holders = (s: string): string[] =>
      [...s.matchAll(/\{(\w+)\}/g)].map((m) => m[1]).sort();
    for (const key of Object.keys(MESSAGES.zh) as MessageKey[]) {
      expect(holders(MESSAGES.en[key]), `key ${key} 的占位符不一致`).toEqual(
        holders(MESSAGES.zh[key]),
      );
    }
  });
});

describe("t", () => {
  beforeEach(() => setLocale("zh"));

  it("按当前语言取值", () => {
    expect(t("toolbar.addChild")).toBe("添加子节点");
    setLocale("en");
    expect(t("toolbar.addChild")).toBe("Add child node");
  });

  it("插值替换 {name}", () => {
    expect(t("notice.createFailed", { error: "EACCES" })).toBe(
      "新建思维导图失败：EACCES",
    );
  });

  it("插值接受数字", () => {
    expect(t("badge.priority", { priority: 3 })).toBe("优先级 3");
  });

  it("多个占位符都替换", () => {
    setLocale("en");
    expect(t("badge.progress", { progress: 60 })).toBe("Progress 60%");
  });

  it("参数缺失时保留 {name} 字面量，不抛异常", () => {
    // 面向用户的文案出问题不该让渲染管线崩掉（见设计文档 5.1）
    expect(t("notice.createFailed")).toBe("新建思维导图失败：{error}");
  });

  it("语言名在两种语言下都用它自己的语言写", () => {
    setLocale("zh");
    expect(t("settings.language.zh")).toBe("简体中文");
    expect(t("settings.language.en")).toBe("English");
    setLocale("en");
    expect(t("settings.language.zh")).toBe("简体中文");
    expect(t("settings.language.en")).toBe("English");
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run tests/i18n.test.ts`
Expected: FAIL —— `src/i18n.ts` 不存在（模块解析错误）。

- [ ] **Step 3: 写 `src/i18n.ts`**

```ts
/**
 * 界面文字的中英双语字典与取值入口。
 *
 * **本模块刻意不 `import obsidian`。** `getLanguage()` 只在 `main.ts` 一处
 * 调用，结果作为参数传进 `resolveLocale`——这样字典与解析逻辑可以直接单测，
 * 不需要 mock Obsidian。
 *
 * **但它也不放进 `src/model/`。** `t()` 读下面那个模块级的 `current`，同样的
 * 入参在 `setLocale()` 前后返回不同结果，它不是纯函数。`check-purity.mjs` 只
 * 校验四条禁令（import obsidian / document / window / HTMLElement），一个字典
 * 加一个可变变量能通过校验，但放进纯函数层是在骗人：那一层的契约是纯粹性，
 * 不是「碰巧没碰 DOM」。
 */

export type Locale = "zh" | "en";
export type LanguageSetting = "auto" | "zh" | "en";

export const LOCALES: readonly Locale[] = ["zh", "en"] as const;

/**
 * 中文表同时充当 key 的唯一来源：`MessageKey` 由它推导，英文表声明为
 * `Record<MessageKey, string>`，于是漏一条英文翻译是编译错误，调用处拼错 key
 * 也是编译错误。
 *
 * `{name}` 是插值占位符，由 `t()` 替换。
 */
const zh = {
  // 命令面板
  "command.toggleView": "切换思维导图 / 源码视图",
  "command.markAsMindmap": "标记为思维导图（写入 frontmatter）",

  // 文件右键菜单
  "menu.newMindmap": "新建思维导图",
  "menu.openAsMindmap": "以思维导图打开",

  // 通知
  "notice.createFailed": "新建思维导图失败：{error}",
  "notice.marked": "已标记为思维导图。",
  "notice.toggleFailed": "切换视图失败：{error}",
  "notice.externalChange": "文件已在外部修改，以磁盘内容为准。",

  // 设置
  "settings.autoOpen.name": "自动以思维导图打开",
  "settings.autoOpen.desc":
    "文件 frontmatter 含 mindmap: true 时，打开即进入思维导图视图。",
  "settings.language.name": "界面语言",
  "settings.language.desc": "插件界面文字使用的语言。切换后立即生效。",
  "settings.language.auto": "跟随 Obsidian",
  // 语言名按惯例始终用它自己的语言书写，两张表里的值相同
  "settings.language.zh": "简体中文",
  "settings.language.en": "English",

  // 视图
  "view.switchToSource": "切换到源码模式",
  "view.displayTitle": "思维导图",
  "view.untitledFile": "未命名.md",
  "view.error.title": "无法解析为思维导图",
  "view.error.hint": "文件未被修改。可切到源码模式检查内容。",
  "view.link.placeholder": "链接目标（笔记名）",

  // 缩放控件
  "controls.fit": "适应窗口",
  "controls.zoomOut": "缩小",
  "controls.zoomIn": "放大",

  // 标记面板的分区标题
  "marks.priority": "优先级",
  "marks.progress": "进度",
  "marks.flag": "旗帜",

  // 角标的无障碍标签，node-el.ts 与 marks-panel.ts 共用
  "badge.priority": "优先级 {priority}",
  "badge.progress": "进度 {progress}%",
  "badge.flag": "旗帜 {flag}",
  "badge.collapsed": "已折叠 {count} 个子节点，点击展开",

  // 工具栏
  "toolbar.style.bold": "加粗",
  "toolbar.style.italic": "斜体",
  "toolbar.style.strike": "删除线",
  "toolbar.addChild": "添加子节点",
  "toolbar.addSibling": "添加兄弟节点",
  "toolbar.addSibling.blockedRoot": "根节点没有兄弟节点",
  "toolbar.remove": "删除节点",
  "toolbar.remove.blockedHidden":
    "该节点携带图上不可见的正文（标题下的散文、有序列表、代码块等），删除会连带丢掉这些内容",
  "toolbar.remove.blockedRoot": "根节点不能删除",
  "toolbar.textStyle": "文字样式",
  "toolbar.marks": "标记",
  "toolbar.marks.blockedRoot": "根节点不能带标记：文件里没有一级标题时无处写回",
  "toolbar.link": "插入链接",
  "toolbar.collapse": "折叠子树",
  "toolbar.collapse.blocked": "没有子节点可折叠",

  // 新建文件的默认基名。它会落到磁盘上成为文件名与文件里的 H1，
  // 只影响新建，已有文件不改名不重写（见设计文档第 6 节）。
  "newFile.basename": "未命名思维导图",
} as const;

export type MessageKey = keyof typeof zh;

const en: Record<MessageKey, string> = {
  "command.toggleView": "Toggle mindmap / source view",
  "command.markAsMindmap": "Mark as mindmap (write frontmatter)",

  "menu.newMindmap": "New mindmap",
  "menu.openAsMindmap": "Open as mindmap",

  "notice.createFailed": "Could not create the mindmap: {error}",
  "notice.marked": "Marked as a mindmap.",
  "notice.toggleFailed": "Could not switch the view: {error}",
  "notice.externalChange": "The file changed externally; the copy on disk wins.",

  "settings.autoOpen.name": "Open as mindmap automatically",
  "settings.autoOpen.desc":
    "When a file's frontmatter contains mindmap: true, open it directly in mindmap view.",
  "settings.language.name": "Interface language",
  "settings.language.desc":
    "Language used for this plugin's interface. Takes effect immediately.",
  "settings.language.auto": "Follow Obsidian",
  "settings.language.zh": "简体中文",
  "settings.language.en": "English",

  "view.switchToSource": "Switch to source mode",
  "view.displayTitle": "Mindmap",
  "view.untitledFile": "Untitled.md",
  "view.error.title": "Cannot read this file as a mindmap",
  "view.error.hint":
    "The file was not modified. Switch to source mode to inspect it.",
  "view.link.placeholder": "Link target (note name)",

  "controls.fit": "Fit to window",
  "controls.zoomOut": "Zoom out",
  "controls.zoomIn": "Zoom in",

  "marks.priority": "Priority",
  "marks.progress": "Progress",
  "marks.flag": "Flag",

  "badge.priority": "Priority {priority}",
  "badge.progress": "Progress {progress}%",
  "badge.flag": "Flag {flag}",
  "badge.collapsed": "{count} child nodes collapsed — click to expand",

  "toolbar.style.bold": "Bold",
  "toolbar.style.italic": "Italic",
  "toolbar.style.strike": "Strikethrough",
  "toolbar.addChild": "Add child node",
  "toolbar.addSibling": "Add sibling node",
  "toolbar.addSibling.blockedRoot": "The root node has no siblings",
  "toolbar.remove": "Delete node",
  "toolbar.remove.blockedHidden":
    "This node carries body text that the map does not show (prose, ordered lists and code blocks under a heading); deleting it would take that content with it",
  "toolbar.remove.blockedRoot": "The root node cannot be deleted",
  "toolbar.textStyle": "Text style",
  "toolbar.marks": "Marks",
  "toolbar.marks.blockedRoot":
    "The root node cannot carry marks: with no level-1 heading in the file there is nowhere to write them",
  "toolbar.link": "Insert link",
  "toolbar.collapse": "Collapse subtree",
  "toolbar.collapse.blocked": "No child nodes to collapse",

  "newFile.basename": "Untitled Mindmap",
};

/** 供测试遍历两张表；生产代码请用 `t()`，不要直接读它。 */
export const MESSAGES: Record<Locale, Record<MessageKey, string>> = { zh, en };

/** 纯函数：设置值 + Obsidian 的语言码 → 实际语言。 */
export function resolveLocale(
  setting: LanguageSetting,
  appLanguage: string,
): Locale {
  if (setting !== "auto") return setting;
  // Obsidian 的 getLanguage() 返回 ISO 码，中文可能是 zh / zh-TW / zh-HK。
  // 只有简体一套表，繁体归到简体（见设计文档第 12 节的已知局限）。
  return appLanguage.startsWith("zh") ? "zh" : "en";
}

/** 模块级当前语言。默认中文，与改造前的行为一致。 */
let current: Locale = "zh";

/**
 * 切换当前语言。
 *
 * **`t()` 自己不通知任何人。** 已经渲染出去的 DOM 不会因为这次调用而更新——
 * 语言变更后必须由 `main.ts` 的 `applyLanguage()` 显式重注册命令并让各导图
 * 视图重建图层。这是本方案唯一的真实陷阱。
 */
export function setLocale(locale: Locale): void {
  current = locale;
}

/**
 * 取当前语言下 key 对应的文案，并替换 `{name}` 占位符。
 *
 * 参数缺失时保留 `{name}` 字面量而不抛异常：面向用户的文案出问题不该让渲染
 * 管线崩掉，留着占位符至少能让人看出是哪条文案缺参数。
 */
export function t(
  key: MessageKey,
  params?: Record<string, string | number>,
): string {
  const template = MESSAGES[current][key];
  if (params === undefined) return template;
  return template.replace(/\{(\w+)\}/g, (whole, name: string) => {
    const value = params[name];
    return value === undefined ? whole : String(value);
  });
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest run tests/i18n.test.ts`
Expected: PASS（12 个）

- [ ] **Step 5: 四条门禁**

```bash
npm run typecheck && npm test && npm run build && npm run check:purity
```

Expected: 全绿，用例数比改动前多 13 个，**没有任何原有用例变红**（本 task 纯新增）。

- [ ] **Step 6: 提交**

```bash
git add src/i18n.ts tests/i18n.test.ts
git commit -m "feat: 加 i18n 模块（中英字典、resolveLocale、t）"
```

---

### Task 2: `model/new-file.ts` — 基名改为必需参数

**Files:**
- Modify: `src/model/new-file.ts`
- Modify: `tests/new-file.test.ts`

**Interfaces:**
- Consumes: 无（本 task 刻意不 import i18n —— 纯函数层不能依赖有可变状态的模块）
- Produces: `uniqueMindmapPath(folderPath: string, basename: string, exists: (path: string) => boolean): string`；`DEFAULT_MINDMAP_BASENAME` **被删除**

- [ ] **Step 1: 改 `src/model/new-file.ts`**

删掉 `DEFAULT_MINDMAP_BASENAME` 这个 export，给 `uniqueMindmapPath` 加第二个参数：

```ts
/**
 * 在 folderPath 下为新思维导图挑一个不重复的路径。
 * 规则与 Obsidian 自带「未命名 / 未命名 1 / 未命名 2」一致：先试无编号，再从 1 起递增。
 * folderPath 为 "/" 或 "" 时表示库根目录，返回的路径不带目录前缀。
 * exists 由调用方注入（通常是 vault.getAbstractFileByPath），保持本模块无 obsidian 依赖。
 *
 * basename 是**必需参数，不给默认值**：它跟随界面语言（见 i18n 的
 * `newFile.basename`），一旦在这里留一个中文默认值，调用方漏传时会静默用中文，
 * i18n 就形同虚设。本模块在纯函数层，不能自己去读语言。
 */
export function uniqueMindmapPath(
  folderPath: string,
  basename: string,
  exists: (path: string) => boolean,
): string {
  const prefix = folderPath === "/" || folderPath === "" ? "" : `${folderPath}/`;
  for (let n = 0; ; n++) {
    const base = n === 0 ? basename : `${basename} ${n}`;
    const path = `${prefix}${base}.md`;
    if (!exists(path)) return path;
  }
}
```

`newMindmapContent(title)` 不动。

- [ ] **Step 2: 改 `tests/new-file.test.ts`**

四个 `uniqueMindmapPath` 用例都要传基名。把原有中文用例保留（证明中文路径仍然工作），并**新增一组英文基名用例**：

```ts
describe("uniqueMindmapPath", () => {
  it("库根目录下不带前缀", () => {
    expect(uniqueMindmapPath("/", "未命名思维导图", () => false)).toBe(
      "未命名思维导图.md",
    );
  });

  it("子目录下用 / 拼接", () => {
    expect(uniqueMindmapPath("笔记/项目", "未命名思维导图", () => false)).toBe(
      "笔记/项目/未命名思维导图.md",
    );
  });

  it("已存在时从 1 开始编号，跳过所有已占用的名字", () => {
    const taken = new Set([
      "未命名思维导图.md",
      "未命名思维导图 1.md",
      "未命名思维导图 2.md",
    ]);
    expect(uniqueMindmapPath("/", "未命名思维导图", (p) => taken.has(p))).toBe(
      "未命名思维导图 3.md",
    );
  });

  it("存在性判断收到的是完整路径", () => {
    const seen: string[] = [];
    uniqueMindmapPath("a/b", "未命名思维导图", (p) => {
      seen.push(p);
      return seen.length < 2;
    });
    expect(seen).toEqual(["a/b/未命名思维导图.md", "a/b/未命名思维导图 1.md"]);
  });

  it("英文基名同样工作，去重编号规则一致", () => {
    const taken = new Set(["Untitled Mindmap.md", "Untitled Mindmap 1.md"]);
    expect(uniqueMindmapPath("/", "Untitled Mindmap", (p) => taken.has(p))).toBe(
      "Untitled Mindmap 2.md",
    );
  });

  it("基名含空格与中英混排时不破坏编号后缀的位置", () => {
    expect(uniqueMindmapPath("/", "My 导图", (p) => p === "My 导图.md")).toBe(
      "My 导图 1.md",
    );
  });
});
```

`newMindmapContent` 那两个 describe 块不动。

- [ ] **Step 3: 修调用方让编译通过**

`src/main.ts` 的 `createMindmapInFolder` 里 `uniqueMindmapPath(folder.path, ...)` 会因为缺参数而编译失败。**本 task 只做最小修复**，暂时传一个中文字面量占位；Task 5 会把它换成 `t("newFile.basename")`：

```ts
    const path = uniqueMindmapPath(
      folder.path,
      // Task 5 会换成 t("newFile.basename")
      "未命名思维导图",
      (candidate) => this.app.vault.getAbstractFileByPath(candidate) !== null,
    );
```

- [ ] **Step 4: 门禁**

```bash
npm run typecheck && npm test && npm run build && npm run check:purity
```

Expected: 全绿，`new-file` 的用例数从 6 增到 8。

- [ ] **Step 5: 提交**

```bash
git add src/model/new-file.ts tests/new-file.test.ts src/main.ts
git commit -m "refactor: uniqueMindmapPath 的基名改为必需参数"
```

---

### Task 3: `src/view.ts` — 7 个 key 与 `refreshLocale()`

**Files:**
- Modify: `src/view.ts`

**Interfaces:**
- Consumes: Task 1 的 `t`
- Produces: `MindmapView.refreshLocale(): void`（公开方法，Task 5 的 `applyLanguage()` 调用它）

- [ ] **Step 1: 换掉 7 处字面量**

顶部加 `import { t } from "./i18n";`，然后逐处替换：

| 位置 | 原文 | 换成 |
|---|---|---|
| `addAction` 的提示 | `"切换到源码模式"` | `t("view.switchToSource")` |
| `getDisplayText()` 的兜底 | `"思维导图"` | `t("view.displayTitle")` |
| 外部修改的通知 | `"文件已在外部修改，以磁盘内容为准。"` | `t("notice.externalChange")` |
| `setViewData` 里的文件名兜底 | `"未命名.md"` | `t("view.untitledFile")` |
| 错误卡片标题 | `"无法解析为思维导图"` | `t("view.error.title")` |
| 错误卡片提示 | `"文件未被修改。可切到源码模式检查内容。"` | `t("view.error.hint")` |
| 错误卡片按钮 | `"切换到源码模式"` | `t("view.switchToSource")` |

用 `grep -n '"切换到源码模式"\|"思维导图"\|"文件已在外部修改' src/view.ts` 确认全部命中，替换后再 grep 一遍应为空。

- [ ] **Step 2: 加 `refreshLocale()`**

放在 `switchToSource()` 附近（都是视图级的公开动作）：

```ts
  /**
   * 语言变更后重建图层，让工具栏、缩放控件、标记面板、错误卡片换上新语言。
   *
   * 置 `layers = null` 再 `render()` 是**唯一允许的重建入口**，与 `setViewData`
   * 完全同一条路径：`render()` 的重建分支靠 `eventsAttached` 守卫防止在
   * `this.root` 上堆叠监听器（AGENTS.md 第 6 条——堆叠会让一个滚轮档位变成
   * `factor^N` 倍缩放）。不要另写一套重建逻辑。
   *
   * 视口与选中保留：`render()` 不动 `camera` 与 `selectedId`。
   */
  refreshLocale(): void {
    this.layers = null;
    this.render();
  }
```

- [ ] **Step 3: 门禁**

```bash
npm run typecheck && npm test && npm run build && npm run check:purity
```

Expected: 全绿，用例数不变（视图层没有自动化测试）。

- [ ] **Step 4: 提交**

```bash
git add src/view.ts
git commit -m "feat: view.ts 的界面文字走 t()，新增 refreshLocale()"
```

---

### Task 4: 四个视图组件的 28 处字符串

**Files:**
- Modify: `src/view/toolbar.ts`（15 处）
- Modify: `src/view/marks-panel.ts`（6 处）
- Modify: `src/view/node-el.ts`（4 处）
- Modify: `src/view/controls.ts`（3 处）

**Interfaces:**
- Consumes: Task 1 的 `t`
- Produces: 无新接口（都是内部字面量替换）

- [ ] **Step 1: `toolbar.ts`**

加 `import { t, type MessageKey } from "../i18n";`（`MessageKey` 下面 `STYLE_MARKERS` 的类型标注要用）。`STYLE_MARKERS` 的 `label` 从常量数组里的固定字符串改为 key，取值时再翻译——否则模块加载时就把语言固定住了，之后 `setLocale` 不生效：

```ts
const STYLE_MARKERS: readonly { marker: "**" | "*" | "~~"; labelKey: MessageKey }[] = [
  { marker: "**", labelKey: "toolbar.style.bold" },
  { marker: "*", labelKey: "toolbar.style.italic" },
  { marker: "~~", labelKey: "toolbar.style.strike" },
];
```

用到它的地方（样式菜单里 `item.textContent = label`）改成 `item.textContent = t(labelKey)`，解构处从 `{ marker, label }` 改为 `{ marker, labelKey }`。

`specs` 数组里 7 个 `label` 与 4 个 `disabledLabel` 全部换成 `t(...)`：

```ts
    { icon: "corner-down-right", label: t("toolbar.addChild"), action: () => handlers.onAddChild() },
    {
      icon: "plus",
      label: t("toolbar.addSibling"),
      action: () => handlers.onAddSibling(),
      disabled: (caps) => !caps.canAddSibling,
      disabledLabel: () => t("toolbar.addSibling.blockedRoot"),
    },
    {
      icon: "trash-2",
      label: t("toolbar.remove"),
      action: () => handlers.onRemove(),
      disabled: (caps) => !caps.canRemove,
      disabledLabel: (caps) =>
        caps.hasHiddenContent
          ? t("toolbar.remove.blockedHidden")
          : t("toolbar.remove.blockedRoot"),
    },
```

`文字样式` → `t("toolbar.textStyle")`、`标记` → `t("toolbar.marks")`、`根节点不能带标记…` → `t("toolbar.marks.blockedRoot")`、`插入链接` → `t("toolbar.link")`、`折叠子树` → `t("toolbar.collapse")`、`没有子节点可折叠` → `t("toolbar.collapse.blocked")`。

**注意 `specs` 是在 `createToolbar()` 函数体内构造的**（不是模块级常量），所以 `t()` 在这里求值拿到的是「创建工具栏那一刻」的语言。这正是 Task 3 的 `refreshLocale()` 要重建图层的原因。在 `specs` 上方加一行注释说明这个依赖。

- [ ] **Step 2: `marks-panel.ts`**

加 `import { t } from "../i18n";`。三个分区标题：`section(panel, "优先级")` → `section(panel, t("marks.priority"))`，进度、旗帜同理。三条角标标签：

```ts
      `优先级 ${priority}`        →  t("badge.priority", { priority })
      `进度 ${progress}%`         →  t("badge.progress", { progress })
      `旗帜 ${flag}`              →  t("badge.flag", { flag })
```

- [ ] **Step 3: `node-el.ts`**

加 `import { t } from "../i18n";`。四处：

```ts
  badge.title = `优先级 ${priority}`;              →  t("badge.priority", { priority })
  wrap.title = `进度 ${progress}%`;                →  t("badge.progress", { progress })
  wrap.title = `旗帜 ${flag}`;                     →  t("badge.flag", { flag })
  badge.title = `已折叠 ${node.children.length} 个子节点，点击展开`;
                                                   →  t("badge.collapsed", { count: node.children.length })
```

`badge.textContent = \`▸ ${node.children.length}\`` **不动**：三角符号加数字不含语言，翻译它没有意义（见设计文档 5.1）。

- [ ] **Step 4: `controls.ts`**

加 `import { t } from "../i18n";`。三处：

```ts
  iconButton(bar, "maximize", t("controls.fit"), () => handlers.onFit());
  iconButton(bar, "minus", t("controls.zoomOut"), () => handlers.onZoomOut());
  iconButton(bar, "plus", t("controls.zoomIn"), () => handlers.onZoomIn());
```

`readout.textContent = "100%"` 与 `` `${Math.round(scale * 100)}%` `` **不动**：纯数字与百分号，不含语言。

- [ ] **Step 5: 门禁**

```bash
npm run typecheck && npm test && npm run build && npm run check:purity
```

Expected: 全绿，用例数不变。

- [ ] **Step 6: 提交**

```bash
git add src/view/toolbar.ts src/view/marks-panel.ts src/view/node-el.ts src/view/controls.ts
git commit -m "feat: 工具栏/标记面板/角标/缩放控件的界面文字走 t()"
```

---

### Task 5: `main.ts` + `settings.ts` — 语言设置、`applyLanguage()`、命令重注册

**Files:**
- Modify: `src/main.ts`
- Modify: `src/settings.ts`
- Modify: `manifest.json`

**Interfaces:**
- Consumes: Task 1 的 `t` / `resolveLocale` / `setLocale` / `LanguageSetting`；Task 2 的 `uniqueMindmapPath(folderPath, basename, exists)`；Task 3 的 `MindmapView.refreshLocale()`
- Produces: `MindmapPlugin.applyLanguage(): void`；`MindmapSettings.language: LanguageSetting`

- [ ] **Step 1: `settings.ts` 加语言设置项**

```ts
import { getLanguage } from "obsidian";
import { t, type LanguageSetting } from "./i18n";

export interface MindmapSettings {
  /** 带 `mindmap: true` 的文件是否自动用思维导图视图打开 */
  autoOpen: boolean;
  /** 界面语言；"auto" 表示跟随 Obsidian 的语言设置 */
  language: LanguageSetting;
}

export const DEFAULT_SETTINGS: MindmapSettings = {
  autoOpen: true,
  language: "auto",
};
```

`getSettingDefinitions()` 增加语言下拉，并把现有那条的文案换成 `t()`：

```ts
  override getSettingDefinitions(): SettingDefinitionItem[] {
    return [
      {
        name: t("settings.language.name"),
        desc: t("settings.language.desc"),
        control: {
          type: "dropdown",
          key: "language",
          defaultValue: DEFAULT_SETTINGS.language,
          options: {
            auto: `${t("settings.language.auto")}（${getLanguage()}）`,
            zh: t("settings.language.zh"),
            en: t("settings.language.en"),
          },
        },
      },
      {
        name: t("settings.autoOpen.name"),
        desc: t("settings.autoOpen.desc"),
        control: {
          type: "toggle",
          key: "autoOpen",
          defaultValue: DEFAULT_SETTINGS.autoOpen,
        },
      },
    ];
  }
```

「跟随 Obsidian」后面括注当前检测到的语言码，让用户看得出跟随的结果是什么。

- [ ] **Step 2: `settings.ts` 覆写 `setControlValue` 作为变更钩子**

`SettingControlBase` 没有 `onChange`，真正的钩子是基类的 `setControlValue`（注释写明「Mutates and persists `this.plugin.settings`」）：

```ts
  /**
   * 声明式设置的写入入口（`@since 1.13.0`）。`SettingControlBase` 没有
   * `onChange`，这里是唯一能观察到「用户改了哪个设置」的地方。
   *
   * 先让基类落盘，再对语言做两件事：`applyLanguage()` 重设语言、重注册命令、
   * 让各导图视图重建图层；`this.update()` 重渲染设置页自身，否则页面上的
   * 标签还留在旧语言。
   */
  override async setControlValue(key: string, value: unknown): Promise<void> {
    await super.setControlValue(key, value);
    if (key !== "language") return;
    this.plugin.applyLanguage();
    this.update();
  }
```

- [ ] **Step 3: `settings.ts` 的 `display()` 兜底路径同步加下拉**

`getSettingDefinitions()` 是 `@since 1.13.0`，而 `minAppVersion` 只抬到 1.8.7，1.8.7–1.12.x 走 `display()`。**两条路径的文案必须一致**，这是最容易只改一处的地方：

```ts
  /** 1.13 之前的版本没有声明式 API，基类回退到这里手工渲染；内容要和上面保持一致。 */
  override display(): void {
    this.containerEl.empty();

    new Setting(this.containerEl)
      .setName(t("settings.language.name"))
      .setDesc(t("settings.language.desc"))
      .addDropdown((dropdown) =>
        dropdown
          .addOption("auto", `${t("settings.language.auto")}（${getLanguage()}）`)
          .addOption("zh", t("settings.language.zh"))
          .addOption("en", t("settings.language.en"))
          .setValue(this.plugin.settings.language)
          .onChange(async (value) => {
            // dropdown 的回调给的是 string，收窄到 LanguageSetting
            this.plugin.settings.language =
              value === "zh" || value === "en" ? value : "auto";
            await this.plugin.saveSettings();
            this.plugin.applyLanguage();
            this.display();
          }),
      );

    new Setting(this.containerEl)
      .setName(t("settings.autoOpen.name"))
      .setDesc(t("settings.autoOpen.desc"))
      .addToggle((toggle) =>
        toggle.setValue(this.plugin.settings.autoOpen).onChange(async (value) => {
          this.plugin.settings.autoOpen = value;
          await this.plugin.saveSettings();
        }),
      );
  }
```

- [ ] **Step 4: `main.ts` 加 `applyLanguage()` 并把命令注册抽成方法**

命令要能重注册，所以注册动作必须可重复调用。把两个 `addCommand` 从 `onload()` 里抽到 `registerCommands()`：

```ts
  /** 两个命令的 id。removeCommand 要用，必须与 addCommand 的 id 逐字一致。 */
  private static readonly COMMAND_IDS = [
    "toggle-mindmap-view",
    "mark-as-mindmap",
  ] as const;

  /**
   * 注册两个命令。可重复调用——`applyLanguage()` 换语言时先移除再注册，
   * 让命令面板里的名字立即变成新语言。
   */
  private registerCommands(): void {
    this.addCommand({
      id: "toggle-mindmap-view",
      name: t("command.toggleView"),
      checkCallback: (checking: boolean) => {
        // 原有的 checkCallback 整段搬过来，一个字符都不改（含那段解释
        // 「为什么用视图反推 leaf 而不是 getMostRecentLeaf」的注释）
      },
    });

    this.addCommand({
      id: "mark-as-mindmap",
      name: t("command.markAsMindmap"),
      checkCallback: (checking: boolean) => {
        // 同上，原有实现整段搬过来
      },
    });
  }

  /**
   * 按当前设置确定界面语言并让它在所有已渲染的界面上生效。
   *
   * `t()` 自己不通知任何人（见 i18n.ts 的说明），所以这里要显式做三件事：
   * 1. 设语言；
   * 2. 重注册命令——`removeCommand` 的 id 必须与注册时逐字一致，写错会变成
   *    重复注册或命令消失，而四条门禁看不到这一点，只能人工验；
   * 3. 让每个已打开的导图视图重建图层。
   */
  applyLanguage(): void {
    setLocale(resolveLocale(this.settings.language, getLanguage()));

    for (const id of MindmapPlugin.COMMAND_IDS) {
      // removeCommand 收的是不带插件前缀的 id（addCommand 会自动加前缀，
      // 见 obsidian.d.ts 对 addCommand 的说明）
      this.removeCommand(id);
    }
    this.registerCommands();

    for (const leaf of this.app.workspace.getLeavesOfType(MINDMAP_VIEW_TYPE)) {
      const view = leaf.view;
      if (view instanceof MindmapView) view.refreshLocale();
    }
  }
```

`onload()` 里原来两段 `addCommand` 的位置改为：

```ts
    // 语言必须在注册命令之前定下来：命令名是注册时求值的。
    setLocale(resolveLocale(this.settings.language, getLanguage()));
    this.registerCommands();
```

顶部 import 加 `getLanguage`（从 `obsidian`）与 `resolveLocale, setLocale, t`（从 `./i18n`）。

- [ ] **Step 5: `main.ts` 换掉其余 5 处字面量**

```ts
  .setTitle("新建思维导图")              →  .setTitle(t("menu.newMindmap"))
  .setTitle("以思维导图打开")            →  .setTitle(t("menu.openAsMindmap"))
  new Notice(`新建思维导图失败：${String(error)}`)
                                          →  new Notice(t("notice.createFailed", { error: String(error) }))
  new Notice("已标记为思维导图。")        →  new Notice(t("notice.marked"))
  new Notice(`切换视图失败：${String(error)}`)
                                          →  new Notice(t("notice.toggleFailed", { error: String(error) }))
```

并把 Task 2 留下的占位符换掉：

```ts
    const path = uniqueMindmapPath(
      folder.path,
      t("newFile.basename"),
      (candidate) => this.app.vault.getAbstractFileByPath(candidate) !== null,
    );
```

- [ ] **Step 6: `manifest.json` 抬 `minAppVersion`**

```json
  "minAppVersion": "1.8.7",
```

`versions.json` **本 task 不动**：它是按发布版本记最低要求的映射，`0.1.1` 那条仍是 `1.5.0`，新条目在发版时随版本号一起加（见设计文档第 8 节）。

- [ ] **Step 7: 门禁**

```bash
npm run typecheck && npm test && npm run build && npm run check:purity
```

Expected: 全绿。

- [ ] **Step 8: 提交**

```bash
git add src/main.ts src/settings.ts manifest.json
git commit -m "feat: 语言设置项与 applyLanguage()，命令名切换后立即生效"
```

---

### Task 6: 第五条门禁 `scripts/check-i18n.mjs`

**Files:**
- Create: `scripts/check-i18n.mjs`
- Create: `scripts/check-i18n.d.mts`
- Modify: `package.json`
- Modify: `.github/workflows/release.yml`
- Modify: `tests/i18n.test.ts`（加注释剥离的单测）

**Interfaces:**
- Consumes: Task 2–5 已把全部字符串搬完（否则本 task 的门禁必然失败）
- Produces: `npm run check:i18n`；从 `scripts/check-i18n.mjs` 导出 `stripComments(src: string): string` 与 `findChineseLiterals(src: string): { line: number; literal: string }[]`，供测试引入

**必须同时写 `scripts/check-i18n.d.mts`。** `tsconfig.json` 的 `include` 覆盖 `scripts`，但没开 `allowJs`，所以从 `.ts` 测试 import 一个 `.mjs` 会让 `tsc --noEmit` 报 `TS7016: Could not find a declaration file`——vitest 能跑通，typecheck 门禁会红。已实测确认：加一个同名 `.d.mts` 之后两者都通过。

- [ ] **Step 1: 写失败测试**

在 `tests/i18n.test.ts` 末尾追加：

```ts
import { findChineseLiterals, stripComments } from "../scripts/check-i18n.mjs";

describe("check-i18n 的注释剥离", () => {
  it("去掉行注释", () => {
    expect(stripComments('const a = 1; // 这是中文注释\n')).toBe("const a = 1; \n");
  });

  it("去掉块注释，含跨行", () => {
    expect(stripComments("/* 中文\n多行 */const a = 1;")).toBe("const a = 1;");
  });

  it("不误删字符串里的 // —— 朴素实现会把 URL 截断", () => {
    const src = 'const u = "https://example.com/中文";';
    expect(stripComments(src)).toContain("https://example.com/中文");
  });

  it("不被注释里的引号带偏", () => {
    const src = '// 注释里有一个 " 引号\nconst a = "干净";';
    expect(stripComments(src)).toContain('"干净"');
  });
});

describe("check-i18n 的中文字面量检测", () => {
  it("命中双引号里的中文", () => {
    expect(findChineseLiterals('const a = "中文";')).toEqual([
      { line: 1, literal: '"中文"' },
    ]);
  });

  it("命中反引号里的中文 —— 漏掉反引号就会整个漏掉 node-el.ts", () => {
    expect(findChineseLiterals("const a = `优先级 ${n}`;")).toHaveLength(1);
  });

  it("不命中注释里的中文", () => {
    expect(findChineseLiterals("// 中文注释\nconst a = 1;")).toEqual([]);
  });

  it("不命中纯英文字面量", () => {
    expect(findChineseLiterals('const a = "ok";')).toEqual([]);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run tests/i18n.test.ts -t "check-i18n"`
Expected: FAIL —— `scripts/check-i18n.mjs` 不存在。

- [ ] **Step 3: 写 `scripts/check-i18n.mjs`**

```js
import { readFileSync, readdirSync, existsSync } from "node:fs";
import { join } from "node:path";

// 扫描范围：视图层与主层。src/model/** 不扫——那一层改造后不含面向用户字符串，
// 而它的中文注释密度最高，扫它只会制造噪音。src/i18n.ts 排除，它就是中文表所在。
const SCAN_DIRS = ["src/view"];
const SCAN_FILES = ["src/main.ts", "src/settings.ts", "src/view.ts"];

const CJK = /[一-鿿]/;

/**
 * 剥离注释，逐字符扫而不是用正则。
 *
 * 朴素的「先把 `//` 到行尾删掉」会把 `"https://example.com"` 截断，
 * 反过来注释里的引号会让「先配对引号」的写法错位。所以必须按状态机走一遍：
 * 任一时刻只可能处于「代码 / 单引号 / 双引号 / 反引号 / 行注释 / 块注释」之一。
 * 注释里的内容替换成等长的空白，保持行号不变。
 */
export function stripComments(src) {
  let out = "";
  let i = 0;
  let state = "code";
  while (i < src.length) {
    const c = src[i];
    const next = src[i + 1];
    if (state === "code") {
      if (c === "/" && next === "/") {
        state = "line";
        i += 2;
        continue;
      }
      if (c === "/" && next === "*") {
        state = "block";
        i += 2;
        continue;
      }
      if (c === '"' || c === "'" || c === "`") {
        state = c;
        out += c;
        i++;
        continue;
      }
      out += c;
      i++;
      continue;
    }
    if (state === "line") {
      if (c === "\n") {
        state = "code";
        out += c;
      }
      i++;
      continue;
    }
    if (state === "block") {
      if (c === "*" && next === "/") {
        state = "code";
        i += 2;
        continue;
      }
      // 保留换行以维持行号
      if (c === "\n") out += c;
      i++;
      continue;
    }
    // 字符串内部：处理转义，遇到同种引号收尾
    if (c === "\\") {
      out += c + (next ?? "");
      i += 2;
      continue;
    }
    out += c;
    if (c === state) state = "code";
    i++;
  }
  return out;
}

/** 在已剥离注释的源码里找出含中文的字符串字面量，返回 { line, literal }。 */
export function findChineseLiterals(src) {
  const clean = stripComments(src);
  const found = [];
  const re = /("(?:[^"\\\n]|\\.)*"|`(?:[^`\\]|\\.)*`|'(?:[^'\\\n]|\\.)*')/gs;
  for (const m of clean.matchAll(re)) {
    if (!CJK.test(m[0])) continue;
    found.push({
      line: clean.slice(0, m.index).split("\n").length,
      literal: m[0],
    });
  }
  return found;
}

const files = [
  ...SCAN_DIRS.filter(existsSync).flatMap((d) =>
    readdirSync(d, { recursive: true })
      .filter((f) => f.endsWith(".ts"))
      .map((f) => join(d, f)),
  ),
  ...SCAN_FILES.filter(existsSync),
];

const errors = [];
for (const file of files) {
  for (const { line, literal } of findChineseLiterals(readFileSync(file, "utf8"))) {
    errors.push(`${file}:${line}: 面向用户的字符串必须走 t()，不要写死中文 → ${literal}`);
  }
}

if (errors.length > 0) {
  console.error(errors.join("\n"));
  console.error("\n（注释不受此约束；若确有非文案用途的中文字面量，把它挪进 src/i18n.ts）");
  process.exit(1);
}
console.log(`界面文字 i18n 校验通过（${files.length} 个文件）`);
```

- [ ] **Step 4: 写 `scripts/check-i18n.d.mts`**

手写的声明文件，与 `.mjs` 的导出保持一致。它会随 `scripts` 目录被 `tsc` 收进来，解决 `TS7016`：

```ts
// scripts/check-i18n.mjs 是 node 直接执行的脚本（与 check-purity.mjs 同款），
// 但它的两个纯函数要被 tests/i18n.test.ts 引用，而 tsconfig 没开 allowJs。
// 这份声明是手写的：改 .mjs 的导出签名时必须同步改这里，两处都只有两个函数，
// 漂移风险可控，但 tsc 不会替你发现漂移。
export declare function stripComments(src: string): string;
export declare function findChineseLiterals(
  src: string,
): { line: number; literal: string }[];
```

- [ ] **Step 5: `package.json` 加脚本**

```json
    "check:purity": "node scripts/check-purity.mjs",
    "check:i18n": "node scripts/check-i18n.mjs"
```

- [ ] **Step 6: 跑测试与新门禁**

Run: `npx vitest run tests/i18n.test.ts -t "check-i18n"`
Expected: PASS（8 个）

Run: `npm run check:i18n`
Expected: `界面文字 i18n 校验通过（N 个文件）`。

**若它报出残留的中文字面量**：那说明 Task 3–5 漏了字符串。**去补 `t()`，不要放宽扫描范围或加豁免名单**——那等于把这道门禁废掉。唯一可接受的例外是确认某个中文字面量不是文案（本仓库目前没有这种情况）。

- [ ] **Step 7: `.github/workflows/release.yml` 加一步**

在「纯函数层边界校验」那步之后插入同样形态的一步：

```yaml
      - name: 界面文字 i18n 校验
        run: npm run check:i18n
```

- [ ] **Step 8: 五条门禁**

```bash
npm run typecheck && npm test && npm run build && npm run check:purity && npm run check:i18n
```

- [ ] **Step 9: 提交**

```bash
git add scripts/check-i18n.mjs scripts/check-i18n.d.mts package.json .github/workflows/release.yml tests/i18n.test.ts
git commit -m "test: 新增第五条门禁 check:i18n，防止界面文字写死中文"
```

---

### Task 7: 文档

**Files:**
- Modify: `AGENTS.md`
- Modify: `README.md`
- Modify: `README.zh-CN.md`
- Modify: `docs/MANUAL-VERIFICATION.md`

**Interfaces:**
- Consumes: Task 1–6 的全部行为
- Produces: 无代码接口

- [ ] **Step 1: `AGENTS.md` 第 10 条推翻旧约束**

把「面向用户的字符串一律中文」这一行替换为：

> - **面向用户的字符串一律走 `t()`（`src/i18n.ts`），不写死任何语言。** 中文表是 key 的唯一来源，英文表声明为 `Record<MessageKey, string>`，漏译是编译错误。`npm run check:i18n` 扫 `src/main.ts` / `src/settings.ts` / `src/view.ts` / `src/view/**` 的字符串字面量，发现中文即失败——**注释不受约束，只有字面量受约束**。
> - `src/i18n.ts` 既不 `import obsidian`（`getLanguage()` 只在 `main.ts` 一处调用），也不放进 `src/model/`（`t()` 有模块级可变状态、不是纯函数）。
> - **`t()` 自己不通知任何人。** 语言变更后必须由 `main.ts` 的 `applyLanguage()` 重注册命令并让各导图视图 `refreshLocale()`。已渲染的 DOM 不会自己更新。

- [ ] **Step 2: `AGENTS.md` 的门禁一节从四条改五条**

```bash
npm run typecheck     # tsc --noEmit
npm test              # vitest run
npm run build         # 生成 main.js
npm run check:purity  # 纯函数层边界
npm run check:i18n    # 界面文字未写死中文
```

同时在「自动化测不到什么」一节补一句：**`check:i18n` 只能保证「没有写死的中文」，保证不了「英文翻译是对的」**。译文质量只能人工看，机器无从判断。

- [ ] **Step 3: `README.md`**

删掉顶部那句 `> **Note on language:** the plugin's user interface is currently Chinese only (command names, tooltips, settings, error messages). There is no i18n yet. Everything below describes behaviour, not UI strings.`

在「Commands」一节后新增：

> ## Language
>
> The interface is available in English and Simplified Chinese. Settings → Rich Mindmap → **Interface language** offers three choices: **Follow Obsidian** (the default — it shows the language code it detected), **简体中文**, and **English**. Switching takes effect immediately, including the names in the command palette.
>
> The default name of a file created by **New mindmap** follows the language too (`Untitled Mindmap.md` / `未命名思维导图.md`). Existing files are never renamed.
>
> `zh-TW` and `zh-HK` fall back to Simplified Chinese. The plugin description in `manifest.json` is English only — Obsidian does not support localized manifests.

「Project status」一节里删掉 `- The UI is Chinese only; no i18n yet.`。

- [ ] **Step 4: `README.zh-CN.md`**

「项目状态」一节里把 `- **界面文字目前只有中文**（命令名、按钮提示、设置项、错误提示）。尚未做 i18n。` 删掉，并新增与英文版对应的一节：

> ## 界面语言
>
> 界面支持英文与简体中文。设置 → Rich Mindmap → **界面语言** 三选：**跟随 Obsidian**（默认，括号里显示检测到的语言码）、**简体中文**、**English**。切换立即生效，命令面板里的命令名也会跟着变。
>
> 「新建思维导图」创建的文件默认名同样跟随语言（`未命名思维导图.md` / `Untitled Mindmap.md`）。已有文件永不被改名。
>
> `zh-TW` / `zh-HK` 归到简体中文。`manifest.json` 里的插件描述只有英文——Obsidian 不支持本地化 manifest。

- [ ] **Step 5: `docs/MANUAL-VERIFICATION.md` 新增一节**

在第 9b 节之后、第 10 节之前插入：

```markdown
## 9c. 界面语言

- [ ] 设置里出现「界面语言」下拉，三个选项：跟随 Obsidian（括号里是检测到的语言码）、简体中文、English
- [ ] 🔴 **切到 English：设置页自身、工具栏按钮提示、缩放控件提示、标记面板的三个分区标题、角标悬停提示、右键菜单、命令面板里的两个命令名，全部变成英文**。命令面板那一项最容易漏——它靠 `removeCommand` + 重新注册，`id` 写错会表现为命令重复出现或直接消失
- [ ] 🔴 切到 English 后新建一份导图：文件名是 `Untitled Mindmap.md`，文件里的 H1 与之一致；再新建一份是 `Untitled Mindmap 1.md`
- [ ] 切回简体中文：以上全部恢复中文；已经创建出来的 `Untitled Mindmap.md` **不被改名**
- [ ] 把 Obsidian 自身语言切成英文、插件设置保持「跟随 Obsidian」：插件界面变英文，且下拉里括注的语言码随之变化
- [ ] 打开一份无法解析的文件（例如把 frontmatter 写坏），在两种语言下各看一次错误卡片：标题、提示、按钮三处都跟随语言
- [ ] 英文界面下逐个悬停工具栏的禁用按钮：四条禁用原因都是英文且句子通顺（`check:i18n` 只能保证没写死中文，保证不了译文质量）
- [ ] 折叠一个节点，英文界面下悬停折叠角标：提示是 `N child nodes collapsed — click to expand`；角标本身仍是 `▸ N`（三角加数字不翻译）
```

- [ ] **Step 6: 门禁**

```bash
npm run typecheck && npm test && npm run build && npm run check:purity && npm run check:i18n
```

- [ ] **Step 7: 提交**

```bash
git add AGENTS.md README.md README.zh-CN.md docs/MANUAL-VERIFICATION.md
git commit -m "docs: i18n 的格式说明、AGENTS 第 10 条推翻、人工验证清单第 9c 节"
```

---

## 收尾

七个 task 完成后：

1. 跑一遍完整的五条门禁，记录 `npm test` 的用例数。
2. 在真实 Obsidian 里走完 `docs/MANUAL-VERIFICATION.md` 第 9c 节。**这一节没走完之前，不要声称这个特性可用**——`check:i18n` 只能证明没有写死的中文，证明不了切换真的生效、也证明不了译文通顺。
3. `scripts/check-i18n.d.mts` 是手写声明，`tsc` 不会发现它与 `.mjs` 的漂移。之后改那两个函数的签名时要记得同步——只有两个函数，风险可控，但值得知道。
4. 版本号与发布不在本计划范围内。发版时 `versions.json` 要为新版本加一条指向 `1.8.7`（`0.1.1` 那条保持 `1.5.0`）。
